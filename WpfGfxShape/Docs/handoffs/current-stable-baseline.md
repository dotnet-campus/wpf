# 当前稳定基线

> 本文只保存恢复工作所需的稳定摘要与最近验证基线。
> 逐项完成历史见 [`progress-completed-work.md`](progress-completed-work.md)，HW render-target 历史见 [`progress-hw-rendertarget.md`](progress-hw-rendertarget.md)。

## 当前状态

- 当前位于 [`../remaining-gap-closure-plan.md`](../remaining-gap-closure-plan.md) 的阶段 1：完成 HW backend 生命周期和直接设备边界。
- DirectX 绑定固定为 Silk.NET 2.23.0；Win32 优先 Microsoft.Windows.CsWin32 0.3.298。
- D3D9/HW 底座已形成大量闭环，但生产 ABI、UCE/资源协议、完整图元绘制和 PresentationCore E2E 尚未完成。
- 当前不能替换原 `wpfgfx_cor3.dll`。

## 稳定能力摘要

- loader、display/device manager、device/resource/use-context、HW render state、surface/texture/swap-chain、registry/display 已形成大量闭环。
- HW display/window/surface render target、软件 3D fallback、3D geometry renderer、shader/fixed-function pipeline、bitmap color source/cache、software rasterizer/render target、glyph/text 窄路径已有可运行实现。
- `CSwRenderTargetSurface` 的 pipeline、intermediate buffers、alpha、SetSurface、current clip 与 Clear/32bpp 格式分派已形成闭环。
- 直接设备边界已覆盖大量 capability、资源创建/更新、状态设置、绘制、render-target、GPU marker query、错误映射与释放后保护。
- 详细完成事实不再复制到当前入口，统一维护在完成进展归档中。

## 最近验证基线

- 本轮完成原生 `CD3DTexture::GetTextureSize` 的 level-0 尺寸快照与生产调用者差集审计。原生只在资源有效期内返回初始化缓存的宽高，不重新调用 D3D9、不进入 device/use context，也不改变 COM 引用或资源使用状态。托管 `Direct3D9Texture` 已缓存相同尺寸，但原自动属性允许释放后读取；现改为带有效性检查的只读访问器，并新增有效读取及宽、高分别在释放后拒绝访问的回归。验证：`Direct3D9TextureTests` 33/33、全量主测试 3859/3859、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DDeviceLevel1::StretchRect` 包装层与非自动 mipmap 消费链的所有权审计：包装只借用源/目标 surface 身份，可空矩形和 filter 原样转发，不增加引用或转移所有权；mipmap 调用者负责逐级引用转移及失败清理。托管实现保持相同语义，无需修改生产或测试代码；详细结论已归档。实际验证：`Direct3D9DeviceStretchRectTests` 与 `Direct3D9TextureTests` 合计 39/39、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CHwD3DBufferSpaceLocator::AdvanceToNextChunk` 与 `ReportNumberOfElementsUsedInLastChunk` 的动态 vertex/index buffer 消费边界审计。原生在每次新申请前先越过上次实际使用字节，并按本次元素大小向上对齐；尾部容量足够时使用 `D3DLOCK_NOOVERWRITE`，不足时使用 `D3DLOCK_DISCARD` 并从元素 0 重新开始。Vertex/Index Lock 分别消费精确字节偏移、长度、lock flags 与 start element；Vertex Unlock 仅在底层 Unlock 成功后按实际使用数量收缩最新区间，Index Unlock 固定保留完整申请量。现有 `Direct3D9BufferSpaceLocator` 与硬件 vertex/index buffer 保持相同语义，既有测试已覆盖首块 NOOVERWRITE、跨 stride 对齐、部分 vertex 使用量、尾部 discard、容量拒绝及查询无副作用，无生产或测试缺口。验证：`Direct3D9HardwareBufferTests` 60/60、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DVertexBuffer`、`GrowVertexBufferSize`、`GetMultipleVertices` 及 DUV2、DUV6、XYZNDSUV4 三个派生 primitive buffer 的生命周期与容量边界审计。原生每次固定申请 4 个顶点；容量从 4 起按 2 倍增长，并在剩余容量恰等于请求量时预先扩容；新容量达到保留上限 `0xFFFF` 前返回 `E_INVALIDARG`，分配失败保持 `E_OUTOFMEMORY`，成功扩容保留既有顶点并清零新增存储；`Clear` 只重置已用顶点数。设备分别持有三个稳定且相互隔离的类型化缓冲区身份。现有托管 `Direct3D9PrimitiveVertexBuffer<TVertex>` 与三个包装类型保持相同固定请求、扩容、HRESULT、数据保留、清空复用和设备所有权语义，无生产实现缺口；既有测试已覆盖精确耗尽容量时扩容和三个缓冲区身份。验证：`Direct3D9DeviceDrawTests` 62/62、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::HandleDIE` 的内联错误映射与 deferred Present 消费边界审计。该 helper 只在输入恰为 `D3DERR_DRIVERINTERNALERROR` 时把原始错误保存到 `m_hrDisplayInvalid`，并向当前调用者返回 `WGXERR_DISPLAYSTATEINVALID`；其他成功、普通失败与 device-lost HRESULT 均原样返回。它不立即销毁设备、不改变 COM 引用，也不执行 render-target 或 scene 清理；后续 Present 才消费保存的原始显示无效原因并进入既有设备不可用流程。现有托管 `HandleDeviceInternalError`、`UnusableReasonHResult`、`PreflightPresent` 与 `MarkUnusable` 保持相同映射、原始原因保存和延迟消费语义，无生产或测试缺口。验证：全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CGPUMarker::GetId` 与设备 active/free marker 消费调用链差集审计。该内联访问器只返回 marker 构造或 `Reset` 提交的 64 位 id，不访问 query、不改变 issued/consumed 状态，也不产生 COM 引用；唯一生产调用者 `CD3DDeviceLevel1::FreeMarkerAndItsPredecessors` 仅在目标 marker 已确认消费后读取该 id，先更新 last-consumed watermark，再把目标及全部前驱从 active 列表转移到 free 列表。现有托管 `Direct3D9GpuMarker.Id` 与设备 marker 队列保持相同值提交、同一对象复用和无引用复制语义，既有回归已覆盖 newest/middle marker 消费、前驱复用、查询失败禁用与 device-lost 归一化，无生产或测试缺口。验证：GPU marker/queued-present/query creation 定向测试 33/33、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::CreateVertexShaderFromResource` 的声明、实现与生产调用者差集审计。该成员从当前模块的 `RT_RCDATA` 资源定位、加载并锁定预编译 DWORD 流，再调用既有 `CreateVertexShader`；资源定位、加载、锁定或设备创建的首个失败 HRESULT 立即返回，函数不取得需要独立释放的资源句柄所有权。全树唯一生产调用者为 `PrepareShaderEffectPipeline`，且只使用 `VS_ShaderEffects20`/`VS_ShaderEffects30`（资源 900/901）；成功返回的唯一 vertex-shader COM 引用直接提交到设备 effect-pipeline 缓存，设备析构分别释放一次，中途后续步骤失败仍由设备持有并最终回收。由于该调用链、资源和缓存全部属于当前明确禁止扩展的 effects/完整 shader 范围，托管层不应新增资源 900/901、effect pipeline 或推测性资源加载 API；现有 `TryCreateVertexShader` 已独立覆盖 Windows `Stdcall` vtable slot 91、HRESULT 首错、错误输出清理和 COM 所有权。生产代码与测试无需修改。验证：vertex-shader 创建定向测试 17/17、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::CreatePixelShaderFromResource` 文本 shader 窄路径审计。原生从当前模块的 `RT_RCDATA` 资源定位、加载并锁定预编译 DWORD 流，再调用直接设备 `CreatePixelShader`；资源定位、加载、锁定或设备创建的首个失败 HRESULT 立即返回，函数自身不取得需要释放的资源句柄所有权。文本 render-state 初始化按 PS 1.1/2.0 与 A8/L8 组合选择连续四个资源，逐项把唯一 pixel-shader COM 引用提交到成员数组，析构统一逐项释放；中途失败由外层对象析构回收已提交项。现有托管实现从嵌入的原生 `Shaders.rc` 解析相同 16 个资源，按相同四项顺序固定字节码并通过 Windows `Stdcall` vtable slot 106 创建 shader；创建失败释放错误返回的临时接口并保留首错，初始化失败释放临时及此前已提交 shader，成功后由设备确定性释放。生产实现无需修改；新增端到端回归确认嵌入资源的 PS 2.0 字节码实际到达 slot 106、四次创建成功并在设备释放时各释放一次。验证：文本 pixel-shader 定向测试通过、全量主测试 3856/3856、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DTexture::RequiresDelayedRelease` 与 `CD3DResourceManager` 跨帧 released-list 生命周期差集审计。原生仅由 texture wrapper 覆写延迟释放要求；`DestroyResources(WithDelay)` 在当前帧把已 unused 的纹理 identity 转移到上一帧 released-list，不调用其底层 D3D 资源释放，下一次 `DestroyReleasedResourcesFromLastFrame` 才确定性消费并释放；`WithoutDelay` 仍在当前轮立即释放。现有托管 `Direct3D9Texture.RequiresDelayedRelease` 与 `Direct3D9ResourceManager` 保持相同转移、下一帧消费、一次性释放和幂等保护；新增纹理级回归确认当前帧 release count 保持 0、下一帧恰好释放一次。验证：`Direct3D9TextureTests` 30/30、全量主测试 3855/3855、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CGPUMarker` 构造、`Reset`、`InsertIntoCommandStream`、`CheckStatus` 与析构的独立生命周期差集审计。原生 marker 借用创建 query 时返回的唯一 `IDirect3DQuery9` 引用并在析构恰好释放一次；`Reset` 仅重置 issued/consumed 状态和 marker id，不替换或增减 query 引用。插入只有在 `Issue(D3DISSUE_END)` 成功后才提交 issued；状态检查在未 issued 时不访问 query，在未 consumed 时以可选 `D3DGETDATA_FLUSH` 调用 `GetData(NULL, 0, flags)`，把 `S_FALSE` 归一化为 `S_OK` 且保持未消费，首次 `S_OK` 后缓存 consumed 并停止后续查询，失败 HRESULT 原样传播。现有托管 `Direct3D9GpuQuery`/`Direct3D9GpuMarker` 保持 Windows `Stdcall` vtable slot 6/7、相同状态提交、唯一 query 所有权和幂等确定性释放；device active/free marker 列表只转移同一 marker 对象，复用时不复制 COM identity。现有生产与测试覆盖完整，无生产或测试缺口。验证：GPU marker 定向测试 21/21、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DLockableTexturePair` 构造、`InitMain`、`InitAux` 与析构的 wrapper 所有权差集审计。原生构造将 main/aux 清空，两个初始化入口均只允许首次设置并对传入 `CD3DLockableTexture` wrapper 各执行一次 `AddRef`；软件 fallback 创建纹理后把引用提交给 pair，再立即释放调用者临时引用。pair 析构按 main、aux 顺序各释放一次，部分 alpha-texture 初始化失败时，已提交的 aux wrapper 仍由包含 pair 的 fallback 对象析构回收。现有托管 `Direct3D9LockableTexturePair` 通过强对象引用持有构造期 main 与一次性初始化的 auxiliary wrapper，禁止重复 auxiliary 初始化；该类型本身不是独立 native-resource owner，因此不应额外复制 COM identity，也不应主动 `Dispose` 调用方仍拥有的 `Direct3D9Texture`。`CreateLock` 与 `Draw` 消费相同对象身份，现有调用约束和测试覆盖初始化、重复拒绝、main/aux 绘制及锁定路径，无生产或测试缺口。验证：lockable texture pair/render-texture 定向测试 18/18、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSurface::Create`、构造与 `Init` 的引用提交和失败清理差集审计。原生工厂先清空输出，分配零引用 wrapper 后立即建立 wrapper 初始引用；构造函数对传入 surface 执行独立 `AddRef`，`Init` 取得并缓存描述、按 pool/format/尺寸/multisample 计算资源大小并注册 manager。初始化失败释放 wrapper，其析构释放独立 surface 引用；生产调用者始终在工厂返回后释放自身临时 surface 引用，因此成功后只留下 wrapper 所有权。现有托管 `Direct3D9Surface.TryCreate` 采用等价的引用转移表达：调用者把新取得的临时 COM 引用交给工厂，工厂不额外 AddRef；描述初始化失败时立即释放该引用且不注册资源，成功时仅在描述和资源大小全部确定后构造并注册 wrapper，最终 `Dispose` 恰好释放一次。需要保留调用者身份的 texture-level/reference 路径则显式 AddRef，并由调用者释放原引用。现有生产调用链与测试已覆盖成功缓存和注册、描述失败释放、单次最终释放以及保留调用者身份的显式 AddRef，无生产或测试缺口。验证：surface 定向测试 37/37、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DTexture` 构造、析构及缓存 surface-level 释放顺序差集审计。构造函数仅把底层 texture 与缓存数组初始化为空；资源失效时 `ReleaseD3DResources` 先释放并清空底层 texture，而对象析构随后按 level 倒序释放缓存的 `CD3DSurface` wrapper、释放缓存数组，并安全容忍已清空的 texture。现有托管 `Direct3D9Texture.ReleaseD3DResources` 保持同一分层：先释放 texture COM identity，再倒序释放缓存 surface wrapper；外部已取得的 surface-level 继续以其独立引用存活，不依赖 texture wrapper。现有测试已覆盖 texture 先于缓存 surface 释放、倒序缓存释放、重复释放和外部持有 surface-level 的独立存活，无生产或测试缺口。验证：`Direct3D9TextureTests` 定向测试、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSwapChain` 尾随 back-buffer 指针数组分配、构造提交及部分初始化失败清理差集审计。原生 placement `operator new(size_t, size_t)` 为 wrapper 后方一次性追加 `cBackBuffers * sizeof(CD3DSurface*)` 空间，并分别检查乘法和加法溢出；构造函数把 `m_rgBackBuffers` 指向该尾随区域、全部清零后持有基础 swap-chain 引用，并可选持有 Ex identity。`Init` 按索引取得独立 back-buffer COM 引用、创建已注册 surface wrapper 后释放临时引用；任一索引失败由 `Create` 释放 candidate，析构只释放已提交的非空 wrapper、基础和 Ex 引用。现有托管 `Direct3D9SwapChain.TryCreate` 以精确长度的托管数组表达同一存储边界，数组创建失败不会泄漏已持有 candidate；逐项初始化失败时确定性释放所有已初始化 surface，再释放 candidate 的基础及 Ex identity，仅全部成功后提交 `_backBuffers` 和输出对象。托管数组天然处理大小溢出/分配失败，不需要复刻原生尾随内存布局；现有测试已覆盖精确计数、部分初始化失败、缓存输出独立引用、释放次序和重复释放，无生产或测试缺口。验证：swap-chain 定向测试 10/10、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::Create` 的初始化失败删除、成功初始引用提交及 device-manager 跟踪前失败清理差集审计。原生静态工厂先清空输出，分配引用计数为 0 的 wrapper；`Init` 失败时直接 `delete` 部分初始化对象并恢复空输出，成功时才 `AddRef` 提交初始唯一引用。device manager 在 render-target 格式验证或跟踪准备失败后先 `Release` 回到 0，再因尚未纳入 pool 列表而显式删除；底层 `IDirect3DDevice9` 临时引用始终在调用链末尾释放。现有托管 `Direct3D9Objects.CreateDevice` 只在 capabilities、Ex identity、dummy back-buffer 与 cache index 全部成功后构造并返回 `Direct3D9Device`，此前失败按逆序释放 cache index、dummy surface、Ex identity 和基础 device；`Direct3D9DeviceManager.CreateInitializedDevice` 在任一初始化失败时确定性 `Dispose`，`CreateDevice` 在 back-buffer 格式验证或跟踪失败时再次保持同一清理边界，仅成功后加入 usable 列表。托管对象不复刻原生内部 0→1 引用计数，而以单一 `IDisposable` 所有权表达同一提交点；现有测试已覆盖 capabilities/dummy 获取失败、初始化失败、格式失败和跟踪失败，无生产或测试缺口。验证：device creation/manager 定向测试 128/128、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DTexture::D3DSurface0Desc` 与 `Levels` 的初始化快照访问及生产调用链差集审计。两个内联访问器只返回 `Init` 阶段缓存的 level-0 `D3DSURFACE_DESC` 与范围为 1..32 的 mip level 数量，不调用 COM、不增减引用、不进入 device/use-context，也不修改资源状态。原生调用者使用该快照创建匹配的 device-bitmap color source、读取 level-0 格式，以及通过 shared handle 在源设备重建宽高、级数、usage、format、pool 一致的纹理。现有 `Direct3D9Texture.TryCreate` 已通过 Windows `Stdcall` vtable slot 13/17 取得 level count 并逐级验证描述，仅在全部验证成功后提交 `Width`、`Height`、`LevelCount`、资源大小和独立 COM 引用；现有 bitmap realization 与 video-memory texture manager 调用链消费对应描述和级数。未发现生产缺口，无需修改生产代码或测试。验证：texture 相关定向测试 54/54、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1` GPU marker 标志状态私有访问器与生产调用链差集审计。`SetGPUMarkersAsEnabled`、`DisableGPUMarkers`、`AreGPUMarkersEnabled`、`SetGPUMarkersAsTested`、`AreGPUMarkersTested`、`SetGPUMarkersAsConsumed` 和 `HaveGPUMarkersBeenConsumed` 只维护 enabled、tested、consumed 三个独立位；禁用仅清除 enabled 并调用 `ResetMarkers`，不清除 tested/consumed。原生调用者限定于 `InsertGPUMarker` 的首次 event-query probe、创建/issue 失败与 backlog 禁用，`GetNumQueuedPresents` 的门控和已消费后队列报告，以及 `IsConsumedGPUMarker` 的消费提交和状态失败禁用。现有 `Direct3D9Device` 的三个布尔状态、`DisableGpuMarkers`、`ResetGpuMarkers`、`InsertGpuMarker`、`GetNumQueuedPresents` 与 `IsGpuMarkerConsumed` 已保持同一状态转换和资源释放顺序；无需公开私有访问器，也无需修改生产代码或测试。验证：GPU marker 定向测试 21/21、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::CheckBadDeviceDrivers` 的初始化期驱动拒绝与 capability 修正边界审计。原生仅在 registry 未要求跳过检查且设备不是 software/reference 时调用该成员；旧驱动或已标记坏驱动返回 `E_FAIL`，不提交后续初始化，成功硬件路径无条件清除缓存 `RasterCaps` 的 `D3DPRASTERCAPS_SCISSORTEST` 位。现有 `Direct3D9DeviceManager.ApplyDeviceDriverWorkarounds` 在 tier 初始化后、纹理格式探测前进入 device protection，验证 display adapter identity，委托 `Direct3D9DeviceDriverWorkarounds.Apply` 保持 software/reference 跳过、旧/坏驱动失败、成功清除 scissor capability，并只在成功后提交能力缓存；初始化异常由外层释放尚未跟踪的设备。`ShouldSkipDriverCheck` 原生存储在限定源码中恒为 false 且无写入者，现有生产调用固定 false 与可测试 helper 参数符合实际生产契约。未发现生产缺口，无需修改代码或测试。验证：driver-workaround 定向测试 7/7、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::CreateRenderTargetUntracked` 返回裸 `IDirect3DSurface9` 的所有权与托管 `Direct3D9UntrackedSurface` 生命周期闭环。原生输出先清空，在 Windows `Stdcall` `IDirect3DDevice9::CreateRenderTarget` 成功后把唯一临时引用直接转移给调用者；失败时释放驱动即使错误返回仍写出的临时指针，保持首错，并仅由 `HandleDIE` 映射 driver-internal-error。现有 `TryCreateRenderTargetUntracked` 已保持 slot 28 参数、非零成功、失败指针清理、输出为空、普通失败/device-lost 原样传播和释放后设备保护；`Direct3D9UntrackedSurface` 独占该引用，首次 `Dispose` 恰好一次 `Release`，重复 `Dispose` 无额外释放，释放后 `Surface` 访问拒绝。新增的重复释放与释放后访问回归已验证，无需修改生产代码，也未引入资源管理器注册或扩展普通 scene/render-target/present 生命周期。验证：render-target creation 定向测试 10/10、全量主测试 3854/3854、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::CreateRenderTarget` 的跟踪 surface 创建、包装与所有权边界审计。该成员先把 width、height、format、multisample type、quality 与 lockable 原样委托给 `CreateRenderTargetUntracked`，再以设备资源管理器调用 `CD3DSurface::Create`；临时 `IDirect3DSurface9` 引用无论包装成功或失败都在 cleanup 释放，wrapper 成功时通过自身独立引用接管底层 surface。`CD3DSurface::Init` 取得描述快照后按 surface 显存大小注册资源，失败保持首错并由析构释放已持有引用。现有 `Direct3D9Device.TryCreateRenderTarget` 已复用 Windows `Stdcall` vtable slot 28 的 untracked 创建路径，再交给 `Direct3D9Surface.TryCreate` 完成描述快照、资源注册、引用转移、失败释放、driver-internal-error 映射和释放后保护；已有测试覆盖精确参数、成功所有权、失败清理与错误映射，未发现生产缺口。本轮未修改生产代码或测试，也未扩展普通 scene/render-target/present 生命周期。验证：render-target creation 定向测试 9/9、全量主测试 3852/3852、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DVertexBuffer::GetMultipleVertices/GrowVertexBufferSize/ReserveMemoryForVertices` 及三个类型化 `GetNewVertices` 调用链审计。原生每次固定请求 4 个顶点，并在请求数大于或等于剩余容量时先增长；容量从 0 起按 4、8、16 的倍增序列扩展，增长循环要求新容量严格大于旧容量加本次请求，达到 `0xFFFF` 前返回 `E_INVALIDARG`，重分配失败保持旧计数且返回失败，成功后新增内存清零并仅在 reserve 时递增顶点数。现有托管缓冲区原先在请求恰好耗尽剩余容量时不增长，现已改为与原生 `>=` 条件一致；其余四顶点请求、倍增、HRESULT、计数提交、Clear 复用及三个类型化 device-owned 调用链保持不变。新增精确耗尽容量时预先增长到 16 的回归。验证：draw 定向测试通过、全量主测试 3852/3852、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSwapChain::Create/Init` 的包装初始化、back-buffer count 获取与失败清理边界审计。`Create` 在 count 为零时通过 `IDirect3DSwapChain9::GetPresentParameters` 获取实际 count，构造函数对主 swap-chain 执行独立 `AddRef` 并尝试取得独立 Ex 引用；`Init` 先以大小 0 注册资源，再按索引和 `D3DBACKBUFFER_TYPE_MONO` 获取每个 back buffer，交给 `CD3DSurface::Create` 包装后立即释放临时 surface COM 引用。任一步失败均保持首错，并由 wrapper 析构释放主/Ex identity 与所有已初始化 back-buffer wrapper；设备 `GetSwapChain` 和 additional swap-chain 调用者始终释放各自临时 swap-chain 引用。现有 `Direct3D9SwapChain.TryCreate` 已通过 Windows `Stdcall` vtable slot 9/1/0/5 保持相同 count 查询、主接口 `AddRef`、可选 Ex QI、固定缓存数组、逐项 MONO back-buffer 包装、临时引用转移、失败清理、资源注册、确定性释放和释放后保护；已有测试覆盖 retrieval、显式/查询 count、失败输出清理、Ex 与 back-buffer 释放顺序，未发现生产缺口。本轮未修改生产代码或测试，也未扩展 GDI/software-DC 或普通 present 生命周期。验证：swap-chain 与 device swap-chain retrieval 定向测试通过、全量主测试 3851/3851、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DLockableTexture::LockRect/UnlockRect` 的公开直接纹理边界及生产调用链差集审计。两个成员只委托 `IDirect3DTexture9` level 0 的 `LockRect`/`UnlockRect` 并原样返回 HRESULT；`LockRect` 保留可空 `RECT*` 与 flags，且仅通过断言限制 managed/internal-managed 或 system-memory pool，不进入 device entry/use-context、不改变 COM 引用或状态缓存。生产调用者既有传具体矩形并使用 `D3DLOCK_NO_DIRTY_UPDATE` 的 lockable texture pair，也有传 `NULL` 锁定整张纹理的 solid-color 与 software bitmap路径；它们保持首错，并按各自原生状态标志执行必要解锁。托管 `Direct3D9Texture.LockRect` 原先强制传非空矩形，现已改为接受可空 `Direct3D9SurfaceRect?`，通过 Windows `Stdcall` vtable slot 19 将 `null` 原样传为 native null pointer，非空矩形、level 0、flags、locked data 与 HRESULT 行为不变；`UnlockRect` 继续通过 slot 20 原样转发 level 0 和 HRESULT并保持释放后保护。新增整纹理 null-rectangle 回归。验证：纹理与 lockable-pair 定向测试 37/37、全量主测试 3851/3851、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSurface::Desc` 的公开只读描述访问边界及生产调用者差集审计。该内联访问器只返回 `CD3DSurface::Init` 通过一次 `IDirect3DSurface9::GetDesc` 成功取得的 `D3DSURFACE_DESC` 快照引用，不执行后续 COM 调用、`AddRef/Release`、HRESULT 转换或状态修改；生产调用者读取 width、height、format、pool 与 multisample 信息，用于 render-target 绑定状态、尺寸/格式判断和中间面选择。现有 `Direct3D9Surface.TryCreate` 已通过 Windows `Stdcall` vtable slot 12 恰好一次取得并缓存完整 `SurfaceDesc`，失败时释放输入引用且不注册资源；`GetDescription` 在已初始化 wrapper 上只读返回缓存并保持释放后保护，`Direct3D9Device.SetRenderTarget` 等调用链已消费该快照。未发现生产缺口，本轮未修改生产代码或测试，也未扩展普通 scene/render-target/present 生命周期。验证：surface 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSwapChain::GetFrontBuffer` 的声明、实现与生产调用者差集审计。该成员只在 `d3dswapchain.h` 中声明；`src/Microsoft.DotNet.Wpf/src/WpfGfx/` 全树没有对应定义或 D3D9 swap-chain 生产调用者，其余同名符号属于独立的 software double-buffered bitmap 类型，不能作为该声明的行为或所有权证据。现有托管 `Direct3D9SwapChain` 不新增推测性 front-buffer API，既有 back-buffer 缓存、独立引用、失败逆序清理、确定性释放和释放后保护保持不变；未触及 GDI/software-DC、普通 present 或 `Reset/ResetEx`。验证：swap-chain 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::WaitForVBlank` 的公开直接设备边界及 `CHwDisplayRenderTarget::WaitForVBlank` 生产调用链差集审计。该路径仅允许 D3D9Ex 设备；首次调用先永久标记 capability 已测试，使用 QPC 计量一次 `IDirect3DDevice9Ex::WaitForVBlank`，只有 HRESULT 成功且耗时严格小于 75ms 才缓存硬件 VBlank 支持，首次失败或超时及后续已判定不支持均返回 `WGXERR_NO_HARDWARE_DEVICE`。支持后的调用继续原样传递 swap-chain index，底层任意失败统一归一化为 no-hardware，最终 `HandleDIE` 因归一化结果不再使 driver-internal-error 标记设备不可用。唯一 HW display 调用者只在 display 状态有效且设备存在时进入 device entry，并固定等待 swap-chain 0；否则直接返回 no-hardware。现有托管 `Direct3D9Device.WaitForVBlank` 与 `Direct3D9SurfaceRenderTarget.WaitForVBlank` 已保持 Ex/测试委托门控、Windows `Stdcall` vtable slot 124、75ms 严格阈值、首次缓存、失败归一化、固定 chain 0、device-entry、display-invalid 门控及释放后保护，既有回归覆盖快速成功、阈值、失败、driver-internal-error、后续调用和 display invalid，未发现生产缺口，本轮不修改生产代码或测试。验证：surface render-target 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::DbgTraceDeviceCreationFailure` 的 debug-only 诊断边界及生产调用者差集审计。该静态函数只在 `DBG` 下通过 `TraceTag(tagError, ...)` 输出两条包含 adapter ordinal、固定设备创建失败前缀、调用者消息和 HRESULT 十六进制值的诊断，不返回或改写 HRESULT，不改变设备、manager、adapter 状态，不执行 COM、引用计数、分配或清理；Release 构建中的 `TRACE_DEVICECREATE_FAILURE` 宏为空。调用点只在设备初始化、level-1 能力测试和 device manager 创建设备失败分支补充诊断，随后仍由原有 `IFC`/`MIL_THR` 控制流传播或映射错误。现有托管创建路径已经保持对应 HRESULT、状态和所有权语义，但没有等价 debug trace；由于该函数不参与生产行为且新增无调用者诊断 API 不构成翻译闭环，本轮不修改生产代码或测试。验证：device-manager 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSwapChain::DbgGetNumBackBuffers` 的 debug-only 缓存计数访问边界及唯一生产调用者差集审计。该内联访问器只在 `DBG` 下返回构造时保存的 `m_cBackBuffers`，不执行 COM、`AddRef/Release`、缓存修改或状态转换；唯一原生调用者位于被当前禁止边界覆盖的 `CD3DDeviceLevel1::PresentWithGDI`，仅断言该 GDI presenter 路径恰有一个 back buffer。现有托管 `Direct3D9SwapChain.TryCreate` 已按显式或 `GetPresentParameters` 取得的非零 count 创建固定长度 cached back-buffer 数组，并逐项获取独立 surface wrapper；`TryGetBackBuffer` 以该数组长度执行越界门控和独立返回引用，初始化失败逆序释放已创建 back buffers 与 swap-chain identity，释放时确定性清理全部缓存。由于唯一调用者属于禁止扩展的 GDI presenter，且该 getter 不参与生产行为，本轮不新增 debug-only API、生产接线或测试。验证：swap-chain 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::DbgGetID3DDevice9` 的 debug-only 身份访问边界及全部调用者差集审计。该内联访问器只在 `DBG` 下返回设备持有的 `m_pD3DDevice` 借用指针，不执行 `AddRef`、COM 调用或状态变更；唯一原生调用者位于 `CD3DSurface::ReadIntoSysMemBuffer` 的 debug 一致性断言中，先由 surface `GetDevice` 获取独立引用，比较指针身份后立即释放。现有托管 `Direct3D9Device.Device` 已提供同一非拥有身份并保持释放后保护，`Direct3D9Surface.GetDevice` 已覆盖 Windows `Stdcall`、成功引用转移、失败输出清理和释放后拒绝；已完成的 readback 生产路径无需引入仅供 debug 断言使用的新 API。本轮不修改生产代码或测试。验证：相关 device/surface 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DLockableTexturePair` 与 `CD3DLockableTexturePairLock` 的公开声明、实现、software fallback 生产调用者及所有权差集审计。原生 pair 构造为空，`InitMain`/`InitAux` 分别对传入 `CD3DLockableTexture` 执行 `AddRef`，析构按 main、aux 顺序释放；`Draw` 在普通模式只绘制 main，在 vector-alpha 模式严格先以 `TBM_APPLY_VECTOR_ALPHA` 绘制 aux，成功后再以 `TBM_ADD_COLORS` 绘制 main并保持首错。stack lock helper 依次 Lock、AddDirtyRect、逐行清零 main/aux，只有完整 LockOne 成功后才标记对应纹理已锁，析构按 main、aux 顺序忽略 Unlock HRESULT。现有托管 pair/lock 的绘制、锁定、dirty、逐行清零、首错和确定性 Unlock 行为已有回归，但该类型目前没有生产调用者，且托管资源模型没有可等价表示原生同一 wrapper 独立引用计数的机制；为避免以新 wrapper 或裸 COM 引用虚构资源所有权，本轮不接入 software fallback，也未修改生产代码或测试。验证：lockable texture pair 定向测试 8/8、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DDeviceLevel1::ResetMarkers` 及 GPU marker 标志内联边界的声明、实现与生产调用者差集审计。原生 reset 先把 last-consumed marker 水位推进到 last-marker，再逐个销毁 free 与 active marker 并清空两组容器；调用者仅为设备析构、已受保护的首次 device-lost 处理和 `DisableGPUMarkers`。现有 `Direct3D9Device.ResetGpuMarkers`、`DisableGpuMarkers`、`MarkUnusable` 与 `Dispose` 已保持相同水位提交、free/active query 确定性释放、通知前清理、重复不可用不重复释放及析构顺序；tested/enabled/consumed 标志的读写也与原生插入、查询和禁用调用顺序一致。未发现新的生产缺口，本轮未修改生产代码或测试。验证：GPU marker 定向测试通过、全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮实现原生 `CD3DSurface::ReadIntoSysMemBuffer` 的最小托管生产闭环。`Direct3D9Surface.ReadIntoSystemMemoryBuffer` 现保持释放后与 use-context 门控、整字节及 D3D 格式映射、checked stride/buffer/矩形运算；managed/system-memory 路径仅同格式直接锁定，default-pool 路径按 WDDM、紧密 stride 和无 clip 条件包装调用方缓冲区，否则创建 system-memory 中间面，并在尺寸或格式不匹配时按原描述创建临时 render target 后依次执行 `StretchRect` 和 `GetRenderTargetData`。CPU copy 按 clip 与 source 交集逐行复制，Unlock 仅在 Lock 成功后执行且不覆盖首错，临时对象确定性逆序释放。bitmap color-source owner/registry 已能基于 realization state 和 texture level 0 独立引用构造现有 pixel copier，不新增公开 API 或生产 ABI，也未接入 software-DC 链。新增 managed 格式门控、clip 逐行复制、Unlock 清理、use-context 与释放后保护回归。验证：相关定向测试通过，全量主测试 3850/3850、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成原生 `CD3DSurface::ReadIntoSysMemBuffer` 最小生产闭环的审计与设计。现有托管 `TryCreateSystemMemoryUpdateSurface`、`TryCreateRenderTargetUntracked`、`StretchRect`、`GetRenderTargetData`、surface Lock/Unlock 和资源管理器 device/use-context 已足够承载实现，无新增直接设备 helper 缺口。明确路径为：仅接受整字节像素格式并计算紧密源 stride；managed/system-memory surface 只允许输出 D3D 格式与源格式一致并借用原 surface；default-pool surface 在 WDDM、输出 stride 等于紧密 stride且无 clip 时可把调用方缓冲区包装为 system-memory surface，否则由 D3D 分配中间 system-memory surface；源尺寸或输出格式不匹配时先按源格式和原 MSAA 参数创建临时 render target，再以 `StretchRect` 裁剪/转换并执行 `GetRenderTargetData`；需要 CPU copy 时锁定零基目标矩形，按每个 clip 与 source 的交集逐行复制，Unlock 仅作清理且不得覆盖首个失败，最后逆序释放 system-memory surface和临时 render target。bitmap color-source 现有 copier 已正确承担 device-entry、use-context、有效范围、buffer inset、surface 借用释放与 HRESULT 传播，但仍缺从 registry/owner 的真实生产构造接线。下一实现可限定为 internal surface 读回方法、最小局部格式映射/溢出校验、独立测试和 bitmap color-source registry/owner 工厂接线，不进入 software-DC 链。本轮未修改生产代码或测试。验证：相关定向测试 52/52、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DTextureSurface` 公开、protected 与 private 声明覆盖差集审计。原生仅有一个 `Create`、无附加状态的构造、取得并快照 surface 描述且以资源大小 0 注册的 `Init`，以及由资源管理器受控调用、释放唯一底层 surface 引用的 `ReleaseD3DResources`；唯一生产调用者是 `CD3DTexture::GetD3DSurfaceLevel`。现有 `Direct3D9Texture.TryGetSurfaceLevel` 与 `Direct3D9Surface.TryCreateTextureLevel` 已保持查询临时引用、wrapper 独立 AddRef、描述失败清理、零大小注册、缓存持有与每次借出独立引用、父 texture 先释放底层 texture 再逆序释放缓存 surface，以及 manager 销毁后的失效和释放后保护。相关边界此前已归档，本轮未发现新的生产缺口，未修改生产代码或测试。验证：texture 定向测试 28/28、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DVidMemOnlyTexture` 公开与 protected 声明覆盖差集审计。两个 `Create` 重载分别覆盖按 default-pool surface 描述调用设备创建后转交 existing-texture 包装，以及直接包装既有 `IDirect3DTexture9`；现有 `Direct3D9Device.TryCreateBitmapTexture`、`Direct3D9Texture.TryCreate` 和 `Direct3D9VideoMemoryTextureManager` 已保持创建 HRESULT、临时底层引用释放、wrapper 独立 AddRef、资源注册、evictable 成功后标记、失败输出为空及 invalid texture 重建顺序。`Init` 仅委托通用 texture 初始化，构造无附加状态，析构依赖 texture 基类释放；bitmap color source、destination texture、texture render target、cache utility 和 vidmem manager 调用者未引入额外 wrapper 契约，effects 调用者超出当前禁止扩展边界。未发现新的生产缺口，本轮未修改生产代码或测试。验证：texture/device-creation/video-memory-manager 定向测试 53/53、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DLockableTexture` 公开与 protected 声明覆盖差集审计。`Create` 的输出清空、初始引用、基类 `Init`、失败释放和底层 texture 引用提交已由 `Direct3D9Device.TryCreateLockableTexture`、`Direct3D9Texture.TryCreate` 及创建回归承载；`LockRect`、`UnlockRect`、`AddDirtyRect` 均固定作用于 level 0，并通过 D3D9 vtable 槽 19、20、21 与 Windows `Stdcall` 保留 HRESULT。原生 pair 的 lock、manual dirty、逐行清零和已锁定对象清理顺序已由 `Direct3D9LockableTexturePairLock` 及失败回归闭环；另一个 `CHwDeviceBitmapColorSource::UpdateSurfaceSoftware` 调用者属于 bitmap color-source 更新链，不改变 lockable-texture wrapper 契约。构造/析构无额外状态，析构继续依赖 texture 基类释放。未发现新的 wrapper 生产缺口，本轮未修改生产代码或测试。验证：texture/pair 定向测试 36/36、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DTexture` 公开与 protected 声明覆盖差集审计。`DetermineUsageAndLevels` 的单级、自动 mipmap 与 StretchRect 全级链已由 bitmap realization 参数计算和回归承载；`GetTextureSize`、level-0 描述和 level count 均为初始化快照/只读访问；对象级与 surface-level accessor、surface cache、`UpdateMipmapLevels` 的自动生成及逐级线性 StretchRect、延迟释放、`Init`/`InitResource` 的 level 校验/资源大小/引用提交，以及 lockable/vidmem-only 派生 wrapper 均已独立闭环。未发现尚未验证的生产缺口，本轮未修改生产代码或测试，也未扩展公开 API、生产 ABI、线程、内存预算或设备生命周期。验证：texture 定向测试 28/28、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DResource` 公开与 protected 声明覆盖差集审计。公开面中的 `GetResourceSize`、`IsValid`、`IsEvictable` 和 `RequiresDelayedRelease` 均为内联或纯状态读取，`SetAsEvictable` 与 `DestroyAndRelease` 已独立归档；protected 构造/析构、`Init`、`Manager`、`Device` 以及 private `ReleaseD3DResources` 回调的可观察生命周期也已由构造注册、manager/device 借用、资源失效、各派生 wrapper 释放和确定性销毁切片闭环。`DbgIsAssociatedWithDevice`、debug frame 字段与 `GetPerfMeterTag` 仅用于 debug/perfmeter；`DEFINE_RESOURCE_REF_COUNT_BASE` 只是多继承类把 `AddRef/Release` 显式转发到既有 `CD3DResource`/`CMILPoolResource` 引用计数实现，不构成新的资源包装生产动作。未发现尚未独立验证的 `CD3DResource` 生产缺口，本轮未修改生产代码或测试，也未扩展引用计数、线程、终结器或公开 ABI。验证：resource-manager 定向测试 199/199、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮完成 `CD3DResourceManager` 公开声明覆盖差集审计。原生公开面仅包含构造/析构、`Init`、`RegisterResource`、`UnusedNotification`、`UnusableNotification`、`Device`、`DestroyAllResources`、`FreeSomeVideoMemory`、`Use`、`EndFrame`、`DestroyResources`、`DestroyReleasedResourcesFromLastFrame`、`EnterUseContext`、`ExitUseContext`、`IsInAUseContext` 与 `InvalidateAndDestroyResource`；逐项核对定义、设备/资源真实调用者、托管 `Direct3D9ResourceManager`/`Direct3D9Resource` 和完成归档后，全部生产可达边界均已独立闭环。private `TryReleaseNonResidentVideoMemoryResourcesInList` 仅有无实现、无调用声明，统计字段与 debug 分支不构成生产缺口。本轮未修改生产代码或测试，未扩展线程、内存预算、设备重绑或公开 ABI。验证：resource-manager 定向测试 199/199、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DDeviceLevel1::CreateAdditionalSwapChain` 与托管 `Direct3D9Device.TryCreateAdditionalSwapChain` 的既有窄边界。原生在清空输出后先检查设备不可用状态，再对超过 `MaxTextureWidth/MaxTextureHeight` 的 back buffer 返回 `D3DERR_OUTOFVIDEOMEMORY`；随后通过 D3D9 `CreateAdditionalSwapChain` 创建底层 COM 对象，并在 wrapper 初始化完成前保留临时引用。托管已保持相同门控和尺寸回退，使用 vtable 槽 13 与 Windows `Stdcall`，成功及非零成功保留原 HRESULT 和 wrapper 所有权；普通失败释放错误返回的非零 COM 输出并保持输出为空，device-lost/driver-internal-error 映射为 display-invalid 并标记设备不可用，wrapper 初始化失败释放 back buffer、wrapper 与底层临时引用且保持首错。设备格式探测以 128x128 lockable secondary swap chain 获取 back buffer 后继续测试，HWND resize 则先释放旧 surface/swap chain、创建新 flipping chain、提交 bounds/尺寸，再获取 back buffer并清空 dirty list；现有生产实现和回归已覆盖该顺序，无需修改代码或测试。验证：全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 上轮复核 `CD3DResource::DestroyAndRelease` 与托管资源确定性销毁边界。原生有效资源先置 invalid，再经 `UnusableNotification` 进入 manager `DestroyResource`；释放回调期间 manager/device identity 与 tracking、内存计数仍有效，回调完成后才 detach、移出列表并结算计数，最后释放调用者持有的资源引用。已失效但仍 managed 的资源跳过重复通知，仅释放调用者引用；manager 主动销毁则先 invalid、释放 D3D 资源并 detach，外部引用后续只负责对象生存期。托管 `Direct3D9Resource.Dispose`、`ReleaseFromManager` 与 `DestroyResourceCore` 已保持相同可观察顺序、单次 D3D release、重复 Dispose 幂等和 detach 后保护；相关失败 detach、嵌套销毁及各 active 状态回归已存在，生产代码与测试无需修改。验证：资源管理器定向测试 199/199、全量主测试 3846/3846、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 上轮关闭 `CD3DSurface::GetDC`、`ReleaseDC` 与托管 `Direct3D9Surface.GetDeviceContext`、`ReleaseDeviceContext` 的普通 surface 直接 COM 边界。原生和托管分别使用 `IDirect3DSurface9` vtable 槽 15/16、Windows `Stdcall`、`HDC*` 输出及值传递 HDC；GetDC 调用前清零输出，仅对 `E_FAIL` 按 GDI handle quota 重解释为 `E_OUTOFMEMORY` 或 `D3DERR_DRIVERINTERNALERROR`，其他 HRESULT 原样传播。修正托管 GetDC 在失败却收到非零 HDC 时向调用者暴露句柄的问题：现改为立即恰好一次 ReleaseDC、忽略清理失败、保持 GetDC 首错并清零输出，与原生 cleanup 一致。格式测试成功取得 HDC 后恰好一次释放且忽略 ReleaseDC 结果；BitBlt 更新链在后续失败时按原生 cleanup 释放仍持有的 DC。新增槽位/参数/HRESULT、失败句柄清理和释放后保护回归。验证：surface 与 BitBlt 定向测试 35/35、全量主测试 3843/3843、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。 

- 上轮关闭 `CD3DSurface::LockRect`、`UnlockRect` 与托管 `Direct3D9Surface.LockRect`、`UnlockRect` 的直接 COM 边界。原生 wrapper 直接把 `D3DLOCKED_RECT*`、`RECT*` 和 flags 传给 `IDirect3DSurface9`，托管使用 vtable 槽 13/14 和显式 Windows `Stdcall`，调用前将输出置零并原样返回 HRESULT，不增加 COM 引用。原生直接调用者在成功 Lock 后要么在正常路径 Unlock，要么以 cleanup/finally 忽略二次清理失败，保持首错；托管 bitmap 更新路径同样只在 Lock 成功后设置锁定标记，后续失败由 finally 恰好一次 Unlock，显式 Unlock 失败则作为当前首错返回。生产代码无需修改；新增矩形/flags/native 输出透传、Lock 失败零输出且不 Unlock、Unlock HRESULT 单次传播及释放后零 native 调用回归。验证：surface 定向测试 26/26、全量主测试 3839/3839、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DSurface::GetSurfaceSize`、初始化时缓存的 `D3DSURFACE_DESC` 与托管 `Direct3D9Surface.GetDescription` 的尺寸读取边界。原生 `Init` 恰好一次调用底层 `GetDesc`，成功后才由缓存描述计算资源大小并注册；`GetSurfaceSize` 只在有效 wrapper 上读取缓存 Width/Height，不产生 COM 调用，全部直接调用者仅为 GDI present 的 back-buffer 尺寸和 depth-stencil state 的尺寸提交。托管普通 surface、swap-chain back-buffer、depth-stencil 与 texture-level surface 均在创建提交前取得描述，后续 `GetDescription` 复用快照；失败初始化释放拥有引用且不注册，释放后即使缓存仍存在也先抛出 `ObjectDisposedException`，不暴露陈旧尺寸。生产代码无需修改；新增普通 surface 一次读取缓存、失败不提交和释放后零 native 调用回归。验证：surface 定向测试 22/22、全量主测试 3835/3835、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DSurface::GetD3DSurfaceNoAddRef`、`ID3DSurface` 与托管 `Direct3D9Surface` native accessor 的借用关系和调用者有效期。原生可空 accessor 直接返回当前 surface identity，供深度模板释放和解绑容错路径在 identity 已清空时跳过底层调用；必须有效 accessor 由断言保证非空，锁定、解锁、DC、更新、render-target 与同步设备调用均只在父 wrapper 有效期内借用，不执行额外 AddRef/Release。托管 `SurfaceForDeviceCall` 与 `Surface` 分别保持可空和必须有效语义，并在释放后拒绝借用；Lock/DC 路径同步使用同一 identity，资源销毁先完成 render-target/depth-stencil 通知再恰好一次释放并清空 native identity。生产代码无需修改；新增可空借用、必须有效借用、同步 Lock/DC、释放后保护及单次释放清空回归。验证：surface 定向测试 19/19、全量主测试 3832/3832、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 texture level surface cache 与 swap-chain back-buffer cache 的借出引用、部分初始化及视频内存计数边界。原生 texture 首次取得 level 时持有 D3D 返回的临时 COM 引用，texture-surface wrapper 构造再 AddRef 并以大小 0 独立注册，临时引用随后释放；缓存保存一份 wrapper 引用，每次调用再 AddRef 返回调用者，父 texture 先释放底层 texture、再逆序释放缓存 wrapper。原生 swap-chain 初始化逐槽取得临时 back-buffer 引用，普通 surface wrapper AddRef 并按描述独立统计大小，临时引用随后释放；缓存借出只增加 wrapper/COM 引用，不重复注册或计数，父 swap-chain 按主接口、Ex、缓存 surface 顺序释放。现有 COM 引用、失败清理、重复取得和释放顺序等价；修正 texture-level wrapper 误按普通 surface 重复统计视频内存的问题，改为描述验证后以大小 0 注册。新增 texture 与 swap-chain 缓存计数回归。验证：缓存定向测试 35/35、全量主测试 3824/3824、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 texture、surface、swap-chain 包装器的创建成功提交与失败清理边界。原生 texture 在验证全部 level 描述并计算总大小后注册，随后保存并 AddRef 底层 texture；surface 构造先 AddRef，`GetDesc` 成功并计算 system-memory/格式/尺寸/多重采样大小后才注册；swap-chain 构造先 AddRef 主接口并可选 QI Ex，随后以大小 0 注册，再逐个取得 back buffer、创建 surface 包装器并释放临时返回引用，任一失败由外层 wrapper Release 触发 `UnusableNotification`，依次释放主接口、Ex 和已提交 back buffer，移除全部 tracking 并回退计数，输出保持 null。现有 texture 和 swap-chain 顺序及失败清理等价；修正 surface 在 `GetDesc` 前以大小 0 提前注册且成功后未计视频内存的问题，改为描述成功后按原生公式计算大小再构造注册，同时保持 transferred/reference 两种 COM 引用语义，描述失败直接释放对应拥有引用且不产生 manager tracking。强化 depth-surface 回归，覆盖 320x240、4x MSAA、D24S8 的 1,228,800 字节统计以及描述失败零计数。验证：包装器定向测试 59/59、全量主测试 3822/3822、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮复核 `CD3DResource::Init`、`CD3DResourceManager::RegisterResource`、`AddToVideoMemoryUsage` 与首次 `Use` 的注册提交边界。原生资源先写 size/valid，再进入 manager；manager 在 thread-protection 断言后把 non-evictable 尾插 non-evictable list，evictable 分支仅在既有 use-context 内可达并先尾插 current-frame in-use list、再由 `Use` 写 debug frame/active depth 和稳定 MRU 位置，随后依次提交 perf meter、外部 media-control 统计、total/peak 与 debug resource count，最后 `CD3DResource::Init` 才保存 manager identity。实际视频内存纹理创建先按 non-evictable 注册，成功返回前才经 `SetAsEvictable` 执行快速 Enter/Use/Exit，因此生产创建链不依赖 evictable-at-registration 分支；owner 访问只发生于 thread-protection 检查、media-control 的 SW-device 判断和首次 `Use` 的 device-entry 断言。现有托管实现以构造前置 owner identity、checked total 预计算、唯一 tracking、non-evictable 注册及后续 `SetAsEvictable` 保持等价失败不部分提交；测试专用 evictable-at-registration 路径也强制 use-context 并完成首次 `Use`。无需修改生产代码。验证：resource-manager 定向测试 199/199、全量主测试 3822/3822、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DResourceManager` 构造、`Init` 与 owner 创建/销毁提交边界。原生构造先形成四条空 active list、空 released/delayed released 状态、零 use-context、零 total/peak 视频内存计数及 debug 初值；manager 是设备内嵌成员，`m_pDevice` 在 `CD3DDeviceLevel1::Init` 最早阶段一次性绑定为 owner 地址，硬件探测失败也会经设备析构清空资源，绑定前没有资源注册或 `Device()` 的生产可达调用。现有托管字段初始化、设备构造时 `this` owner 注入以及先销毁资源/关闭 manager、再释放底层设备的顺序等价；既有独立空 owner 和设备 owner 零状态回归已覆盖，无需修改代码。验证：resource-manager 定向测试 199/199、全量主测试 3822/3822、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮复核 `CD3DResourceManager::TryReleaseNonResidentVideoMemoryResourcesInList` 的原生可达性。唯一允许的原生目录全树仅有 `d3dresource.h` 中一个 private `void` 声明及 `LIST_ENTRY*` 参数，不存在实现、条件编译定义、调用、同名变体或其他 non-resident 资源管理路径；可达回收仍由既有 `FreeSomeVideoMemory`、released-list、active eviction 与 `DestroyAllResources` 完成。因无实现，不能推导输入列表语义、状态变化、失败或返回契约；托管无对应行为且无需修改。验证：全量主测试 3822/3822、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮复核 `CD3DResourceManager::EndFrame` 的 evictable 列表迁移与整体失败边界。原生只在 use-context 全部退出后把 current-frame not-in-use 链整体追加到 previous-frames 尾部并清空当前链，要求 current-frame in-use 为空；空帧保持历史顺序。现有托管实现已先检查 use-context/in-use，再执行迁移、清空和完成帧计数递增，无需修改；新增嵌套 use-context 回归，确认内层已退出形成 not-in-use、外层仍 in-use 时失败不部分迁移且不递增计数，后续成功只迁移一次并保持 LRU 顺序。验证：全量主测试 3822/3822、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DDeviceLevel1::CleanupFreedResources` 与 `AdvanceFrame` 的调用次序、帧号幂等和失败状态提交边界。原生 cleanup 固定先消费上一帧 released-list，再以 `WithoutDelay` 消费当前帧列表且不调用 `EndFrame`；`AdvanceFrame` 仅在帧号变化时先提交新帧号，再依次执行 `EndFrame`、上一帧消费和当前帧 `WithDelay` 消费，因此任一阶段失败后相同帧号均不重试，未消费 identity 由既有失败恢复留待后续不同帧消费。托管生产实现已与该顺序一致，无需修改；强化 `EndFrame` 失败后的同帧无操作，并新增上一帧释放失败后帧号已提交、同帧不重试、下一帧一次性恢复消费的回归。验证：全量主测试 3821/3821、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DResourceManager::UnusedNotification`、`UnusableNotification` 与 `DestroyReleasedResourcesFromLastFrame` 的 released-list 所有权转移和失败消费边界。原生 `UnusedNotification` 只把 identity 压入线程安全 SList；`DestroyResources` 先原子 flush 当前帧列表，按 SList 头序消费并把要求延迟的 identity 头插到上一帧列表；`DestroyReleasedResourcesFromLastFrame` 先转移并清空上一帧头，再独占消费快照，释放回调造成的新 unused identity 进入新的当前帧列表，嵌套 `DestroyResource` 在原生 debug 契约中禁止。托管已有身份去重、逆序消费、延迟转移和 pending 队列，但快照清空后若回调抛出会丢失尚未消费的 identity；现为当前帧和上一帧 flush 增加异常恢复，仅将仍受管理、未 pending 的未消费 identity 放回原队列，保留原异常且不重复已消费资源，新增两条失败后重试一次性清空回归。验证：资源管理器定向测试 197/197、全量主测试 3820/3820、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DResource` 析构前 detach 不变量及所有 manager 销毁入口的顺序边界。原生 `~CD3DResource` 断言 `m_pManager == NULL`；主动 `DestroyAndRelease`、released-list、active-list、`InvalidateAndDestroyResource` 与显式逐出均最终进入 `DestroyResource`，固定先执行 `ReleaseD3DResources()`，再清空 manager、移出 tracking，最后才允许 `delete`。托管普通、延迟、批量、嵌套与重复释放已有相同顺序，但释放回调抛出时原实现会跳过 detach 和 tracking removal；现以 `finally` 保证回调失败仍解除 manager 关联、移除注册并结算内存，同时保留原异常，新增失败 detach 与失败后重复 Dispose 两条回归。验证：全量主测试 3818/3818、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮关闭 `CD3DResource::Manager()` 与 `Device()` 的受保护反向访问边界。原生两者均为 protected 借用访问器：资源关联期间 `Manager()` 断言 `m_pManager` 非空并返回同一 manager 引用，`Device()` 仅转发 `Manager().Device()`；`SetAsEvictable`、资源自身 D3D 释放及 debug 归属检查均只在关联有效期内调用。`CD3DResourceManager::DestroyResource` 严格先调用 `ReleaseD3DResources()`，再把 `m_pManager` 清空并移出 tracking，因此释放回调期间 manager/device 仍可借用，清空后没有原生可达调用。现有托管 `Direct3D9Resource.Manager/Device` 保持同一非拥有 identity，`DestroyResourceCore` 也先释放再 detach，并在 detach 后以 `ObjectDisposedException` 拒绝反向访问，生产代码无需修改；新增 manager/device identity、释放回调有效期及 detach 后两项释放后保护共 5 条回归。验证：全量主测试 3816/3816、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 本轮按 `CD3DDeviceLevel1`/`CD3DResourceManager` 声明与完成归档覆盖差集，关闭 `CD3DResourceManager::Device()` 的 owner 反向身份访问边界。原生 manager 在 `Init(CD3DDeviceLevel1*)` 中仅保存设备成员地址，内联 `Device()` 直接解引用并返回同一 owner 引用；该指针用于资源线程保护与 `CD3DResource::Device()` 转发，不执行 COM `AddRef/Release`、不创建包装、不进入 device/use-context，也不改变资源列表或设备状态。现有托管 `Direct3D9ResourceManager.Device` 已保存同一非拥有设备 identity，设备构造也以 `this` 初始化内嵌 manager，生产代码无需修改；新增设备 owner identity 与独立 manager 空 owner 两项回归。验证：全量主测试 3811/3811、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 当前工作项曾再次指向早已完成归档的 `IDirect3DDevice9::SetDialogBoxMode`。本轮重新核对唯一允许的原生目录后，结论与既有归档一致：全树仅命中 AV `CD3DDeviceWrapper` 的 `NOTIMPL_METHOD` 接口占位，以及独立 ShaderGen `CFakeDevice` 的 `BOOL` 声明和 `STUB` 故障桩；`CD3DDeviceLevel1`、display/device manager、HWND/surface render-target 与当前生产配置均无包装、调用者、BOOL 状态拥有者或 HRESULT 消费者，因此 Release、Debug 与 validation 路径均不会进入底层 setter。现有四项目也没有推测性托管承载，故不新增 slot 20、Windows `Stdcall` 委托、设备状态副作用、测试或相邻 cursor/fullscreen/window management。生产代码与测试无需修改；下一轮改为审计 `CD3DDeviceLevel1` 声明与完成归档的覆盖关系，避免再次领取已完成切片。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对底层 `IDirect3DDevice9::GetTransform` 与 WPF transform 状态缓存边界的复核已确认：精确 `->GetTransform(` 搜索只命中 `CD3DRenderState` 到 `CHwRenderStateManager` 的缓存委托、`CMilEffectDuce::SetupVertexTransform` 经 `CD3DDeviceLevel1::GetTransform` 读取 projection 缓存，以及资源模型自身的同名 getter；没有任何生产调用直接进入 `IDirect3DDevice9::GetTransform`。底层 COM getter 仅存在于 AV `CD3DDeviceWrapper` 的 `NOTIMPL_METHOD` 接口占位和独立 ShaderGen `CFakeDevice` 的 `STUB` 故障桩。真实设置链以 slot 44 `SetTransform` 成功后更新 `CStateTable`：world 只映射 `D3DTS_WORLD` 到单元素 `m_worldTransform[0]`，state `< 256` 映射到 `m_nonWorldTransforms[state]`，受支持项为 view、projection 与 texture0..7；未知项由 `CStateTable::GetState` 返回 `E_FAIL`，已知项复制矩阵输出。Effect、2D/3D vertex-shader 常量路径读取的均是这些 WPF 自有缓存，不通过 driver getter恢复状态。现有托管 `Direct3D9Device.GetTransform` 已保持释放后保护、world/non-world 索引、未知状态 `E_FAIL`、矩阵输出和成功后缓存语义，故不新增 D3D9 vtable slot 45、Windows `Stdcall` COM getter、生产代码或测试。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对底层 `IDirect3DDevice9::GetTexture` 与 WPF texture identity/状态缓存边界的复核已确认：精确调用搜索没有任何 `->GetTexture(` 命中；device COM getter 仅存在于 AV `CD3DDeviceWrapper` 的 `NOTIMPL_METHOD` 接口占位和独立 ShaderGen `CFakeDevice` 的 `STUB` 故障桩，`CD3DDeviceLevel1` 与 HW 生产实现没有对应 getter 包装或调用者。生产纹理绑定只经 `CD3DDeviceLevel1::SetTexture/SetD3DTexture`、`CD3DRenderState::SetTexture` 与 `CHwRenderStateManager::SetTextureInline/ForceSetTexture` 前向设置；`CStateTable<IDirect3DBaseTexture9 *> m_textures` 仅保存非拥有 identity 用于重复设置去重，设置失败即标记未知，不通过 COM getter恢复状态。`CD3DTexture::GetD3DBaseTexture` 是显式 AddRef 的对象级拥有输出，`GetD3DTextureNoRef`、glyph/bitmap/render-target 的 `GetTextureNoRef/GetTextureNoAddRef` 与 `GetTextureSize` 均为对象级借用或尺寸访问，不等同于 device getter。现有托管 `SetD3DTexture/ForceSetTexture` 的非拥有 identity 缓存、失败重试和 slot 65 `Stdcall` setter 语义正确，故不新增 device vtable slot 64、COM 输出、AddRef/Release、生产代码或测试。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对底层 `IDirect3DDevice9::GetRenderState` 与 WPF 自有状态缓存读取边界的复核已确认：唯一看似真实的 HW 设备读取位于 `CD3DRenderState::AssertAlphaBlendMode`，但该方法仅在 `#if DBG` 下声明与编译，Release 下为空内联，且全树没有调用点；方法内部按 `AlphaBlendMode::Type(i)` 执行的底层 `GetRenderState` 又完整包裹在 `#if 0` 中，注释明确说明因 D3D9 debug runtime 的 `GetXXXState` 缺陷而禁用，因此 Debug、Release 和 validation 配置均不会生成该 COM 调用。其余底层 getter 命中仅为 AV `d3ddevicewrapper.h` 的 `NOTIMPL_METHOD` 接口占位和独立 ShaderGen `CFakeDevice` 的 `STUB` 故障桩。生产 `CD3DRenderState::GetFillMode/GetDepthTestFunction` 则调用 `CHwRenderStateManager::GetRenderState`，后者只委托 `CStateTable<DWORD>::GetState` 读取 WPF 自有缓存，不触发 COM。现有托管 `Direct3D9Device.GetRenderState` 正确保持该缓存边界、失败时零值和释放后保护，故不新增 device vtable slot 58、底层委托、测试或相邻 getter。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对 `GetRasterStatus` 的复核已区分接口包装、调用者与工具桩：`CD3DDeviceLevel1` 在 `d3ddevice.h` 中提供内联 device-level 包装，断言 `D3DCAPS_READ_SCANLINE` 后通过 `IDirect3DDevice9::GetRasterStatus` 原样传递 swap-chain index 和输出结构；除此之外，唯一 device-level 命中是 AV `d3ddevicewrapper.h` 的 `NOTIMPL_METHOD` 接口占位和独立 ShaderGen `CFakeDevice` 的 `STUB` 故障桩。全树没有 `CD3DDeviceLevel1::GetRasterStatus` 调用者，也没有 `IDirect3DSwapChain9::GetRasterStatus` 生产调用，因此该包装在当前 WpfGfx 生产链中未被消费。现有托管 `Direct3D9Device.GetRasterStatus` 已准确保持 Windows `Stdcall`、device vtable slot 19、`uint` swap-chain index、默认输出、HRESULT 原样返回和释放后保护，既有测试也已覆盖该窄契约；本轮不删除既有承载、不新增 swap-chain getter、present context、线程、终结器、`Reset/ResetEx` 或生产 ABI。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对 `IDirect3DDevice9::Reset` 与 `IDirect3DDevice9Ex::ResetEx` 的核对已确认当前 WpfGfx 生产路径不可达：基础 `Reset` 仅存在于 AV `d3ddevicewrapper.h` 的单参数 `NOTIMPL_METHOD` 接口占位，以及独立 ShaderGen `CFakeDevice` 的单参数 `STUB` 故障桩；`ResetEx` 在唯一允许的原生目录内零命中。`CD3DDeviceLevel1` 虽缓存可选 `IDirect3DDevice9Ex` identity，但只调用 `ComposeRects`、`CheckDeviceState` 与 `WaitForVBlank` 等已有 Ex 方法，不调用 `ResetEx`。`CD3DDeviceManager` 先尝试复用可用设备，失败时通过 `CreateDeviceEx` 或 `CreateDevice` 创建新的 device identity并构造新的 `CD3DDeviceLevel1`，创建期间检测显示状态变化并在失败路径释放临时接口，不执行旧设备就地 Reset。因此不新增基础 slot 16、Ex `ResetEx` 委托、托管生产 API、测试或状态恢复协议，并继续保持禁止就地 `Reset/ResetEx` 的执行规则。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对 device-level `IDirect3DDevice9::Present` 的核对已确认当前 WpfGfx 生产路径不可达：AV `d3ddevicewrapper.h` 仅保留四参数 `NOTIMPL_METHOD` 接口占位，独立 ShaderGen `CFakeDevice` 仅保留四参数 `STUB` 故障桩；真正的 HW 生产链由 `CHwDisplayRenderTarget::PresentInternal` 调用 `CD3DDeviceLevel1::Present`，再由 `PresentWithD3D` 调用 `IDirect3DSwapChain9::Present`，以 source/destination rect、目标 HWND、dirty region 和 flags 执行 swap-chain 呈现。托管现有 `Direct3D9SwapChain.Present` 已保持 Windows `Stdcall`、swap-chain vtable slot 3、五参数和释放后保护，因此不新增 device-level slot 17 委托、生产 API、测试或第二套呈现链，也不扩展 present context、媒体呈现、GDI/software-DC、完整 present pipeline、线程、终结器或生产 ABI。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生目录全树对 `IDirect3DDevice9::GetGammaRamp` 的核对已确认当前 WpfGfx 生产路径不可达：仅命中 AV `d3ddevicewrapper.h` 中直接 `Assert(FALSE)` 后返回的接口占位，以及独立 ShaderGen 工具 `CFakeDevice` 中直接触发 `BREAK` 的故障桩声明与实现；`CD3DDeviceLevel1`、HW render-target、display/device manager 和其他生产调用链均无调用，托管四项目也无 `GammaRamp` 承载。两处接口均保持 D3D9 的 `void` 返回契约，不存在可传播或映射的 HRESULT，因此不新增 D3D9 vtable 委托、生产 API、测试、`SetGammaRamp`、gamma 控制策略、显示校准、GDI/software-DC 或完整 present pipeline。验证：全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetFVFInline/ForceSetFVF` 与托管 `SetFlexibleVertexFormat/ForceSetFlexibleVertexFormat` 的 FVF 单值状态缓存边界已确认一致：通过 D3D9 vtable slot 89 和 Windows `Stdcall` 原样传递单个 `DWORD/uint` FVF；状态表初始化为 unknown，因此初始零值仍提交。成功及非零成功后缓存完整 32 位值并对相同已知值去重，强制入口始终提交；失败后缓存置 unknown，replacement 失败不会恢复旧值，后续旧值与相同目标均可重试。普通失败、device-lost 与 driver-internal-error在该 render-state manager 边界原样返回且不直接执行 `HandleDIE`；缓存是纯值状态，不涉及 vertex declaration 或 COM 所有权。普通、强制和查询入口均具备释放后保护。现有生产实现和测试无需修改。验证：FVF state 聚焦 24/24、全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetVertexShader/ForceSetVertexShader`、`SetPixelShader/ForceSetPixelShader` 与 `CStateTable` 已确认只缓存 device-owned D3D shader 状态的非拥有 identity：vertex/pixel 分别通过 D3D9 vtable slot 92/107 和 Windows `Stdcall` 原样传递可空 shader 指针；初始 null 仍提交底层，成功及非零成功后缓存 identity并对相同已知 identity 去重，强制设置始终提交。失败后对应缓存变为 unknown，replacement 失败不会恢复旧缓存，后续旧 identity 与相同目标均会重试；replacement 与 null 解绑不由状态表执行 AddRef/Release。普通失败、device-lost 与 driver-internal-error在该 render-state manager 边界均原样返回且不直接触发 `HandleDIE`，设备包装层的映射属于调用者边界。状态表析构只释放表内存，shader COM 引用由 D3D device 自身持有并随 device Release 释放；托管缓存同样不独立拥有 shader 引用，设备 Dispose 不额外 Release shader，普通和强制入口均具备释放后保护。现有生产实现和测试无需修改。验证：shader state 聚焦 32/32、全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::CreateVertexShader/CreatePixelShader` 与托管 shader wrapper 的直接设备创建和 COM 生命周期边界已闭环：分别通过 D3D9 vtable slot 91/106 和 Windows `Stdcall` 调用，shader function 指针原样传递；成功及非零成功均把返回的唯一 owning COM 引用转移给 wrapper并原样返回 HRESULT，失败时即使驱动写回非空指针也先释放。普通失败与 device-lost 原样传播且不新增不可用副作用，driver-internal-error 经既有 `HandleDIE` 映射为 display-state-invalid并记录原始原因。vertex/pixel wrapper 显式 Dispose 各释放一次，重复 Dispose 幂等，两类 native identity 均具备释放后保护；不扩展 shader bytecode 资源定位、shader-effect/resource-manager 新所有权、完整 draw pipeline、线程、终结器或生产 ABI。现有生产实现无需修改；补充两类 wrapper 重复 Dispose 回归。验证：shader creation 聚焦 17/17、全量主测试 3809/3809、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::CreateVertexBuffer/CreateIndexBuffer` 与托管 buffer wrapper 的直接设备创建和 COM 生命周期边界已闭环：分别通过 D3D9 vtable slot 26/27 和 Windows `Stdcall` 调用，length、usage、FVF/format、pool 原样传递且 shared handle 固定为空；成功及非零成功均把返回的唯一 owning COM 引用转移给 wrapper并原样返回 HRESULT，失败时即使驱动写回非空指针也先释放。普通失败与 device-lost 原样传播且不新增不可用副作用，driver-internal-error 经既有 `HandleDIE` 映射为 display-state-invalid并记录原始原因。vertex/index wrapper 显式 Dispose 各释放一次，重复 Dispose 幂等，两类 native identity 均具备释放后保护；不扩展 resource-manager 新所有权、完整 draw pipeline、线程、终结器或生产 ABI。现有生产实现无需修改；补充 index 失败写回指针、两类重复 Dispose和 vertex identity 释放后回归。验证：buffer creation 聚焦 18/18、全量主测试 3808/3808、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetStreamSource/ForceSetStreamSource`、`SetIndices/ForceSetIndices` 与 `CStateTable` 已确认只缓存 device-owned D3D 状态的非拥有 identity：状态表明确不负责对可 AddRef 对象保持引用，vertex stream 仅在底层成功及非零成功后缓存 buffer 与 stride，失败时两项均变为 unknown 并允许重试；同一已知 stream identity/stride 去重，replacement 与 null 解绑都只提交底层设备，不由状态表 AddRef/Release。index stream 的公开去重判断存在，但原生 `ForceSetIndices` 不更新 `m_indexStream`，因此现有托管 `SetIndices` 每次提交底层与原生当前行为一致。状态表析构只释放表内存，buffer 引用由 D3D device 自身持有并在 device Release 时释放；托管缓存同样不独立拥有 COM 引用，设备 Dispose 无额外 buffer Release，重复 Dispose 幂等，释放后入口拒绝。现有生产实现和测试无需修改。验证：stream/index state 聚焦 25/25、全量主测试 3804/3804、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DModuleLoaderInternal::CreateD3DObjects/CleanupD3DReferences` 与托管 `Direct3D9Objects` 的 factory 容器生命周期已闭环：基础 `IDirect3D9` 与可选 `IDirect3D9Ex` 是两个独立 owning COM 引用，即使地址相同也各 Release 一次；释放顺序固定为 Ex、base、D3D9 module handle、software-rasterizer loader。factory 创建的部分初始化失败继续按 Ex、base、module 逆序清理。`Direct3D9Objects` 重复 Dispose 幂等，基础与 Ex identity 属性在释放后均拒绝访问；构造函数允许注入既有 software-rasterizer loader 以隔离验证实际已加载 loader 的释放顺序，不扩展注册策略、线程、终结器或生产 ABI。验证：factory lifetime 聚焦 6/6、全量主测试 3804/3804、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::InsertGPUMarker` 的 event-query capability probe、实际创建与托管 `Direct3D9GpuQuery` COM 生命周期边界已闭环：通过 `IDirect3DDevice9` vtable slot 118 和 Windows `Stdcall` 调用，probe 固定传递 `D3DQUERYTYPE_EVENT` 与 null 输出；实际创建的成功及非零成功均把唯一 owning COM 引用转移给 query wrapper，失败时即使驱动写回非空指针也先释放，成功但返回空指针按失败关闭本次 marker。device-lost/not-available 创建失败保持 marker 功能可重试，其他失败（包括 driver-internal-error）关闭 marker 功能，外部 `InsertGpuMarker` 依原生语义归一为成功；显式 Dispose 释放一次，重复 Dispose 幂等，释放后 Issue 受保护。现有生产实现无需修改；新增非零成功、可恢复/不可恢复失败、重复 Dispose 与释放后访问回归。验证：GPU query creation 聚焦 12/12、全量主测试 3798/3798、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::CreateStateBlock` 与托管 `Direct3D9StateBlock` 的直接设备和 COM 生命周期边界已闭环：通过 `IDirect3DDevice9` vtable slot 59 和 Windows `Stdcall` 调用；成功与非零成功均把返回的唯一 owning COM 引用转移给 wrapper并原样返回 HRESULT；失败时即使驱动写回非空指针也先释放，再保持普通失败和 device-lost HRESULT，driver-internal-error 通过既有 `HandleDIE` 等价映射为 display-state-invalid 并记录原始不可用原因。显式 Dispose 释放一次，重复 Dispose 幂等，释放后 native identity 访问受保护；不引入 resource-manager 所有权，也不扩展 BeginStateBlock/EndStateBlock、Apply/Capture 或生产 ABI。现有生产实现无需修改；新增重复 Dispose 回归。验证：state-block 聚焦 9/9、全量主测试 3793/3793、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwPixelShaderEffect::Create/Init/ReleaseD3DResources`、析构及 `CD3DResourceManager::DestroyResource` 调用链已闭环：底层 pixel shader 创建成功后，其返回 COM identity 的唯一 owning 引用转移给 device-managed shader-effect；debug device identity 仅为非拥有引用。创建 shader 成功但托管资源注册或构造失败时，临时 shader wrapper 在 finally 中逆序释放；资源显式 Dispose 或 manager 主动销毁均只释放 pixel shader identity 一次，移出资源跟踪并归零估算资源大小，重复 Dispose 幂等，pixel shader 与 device identity 访问均受释放后保护。新增 `Direct3D9PixelShaderEffect` 生命周期实现，不扩展 shader bytecode、effect list 或 effects 消费链。验证：shader creation/effect 聚焦 16/16、全量主测试 3792/3792、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwBitmapCache::ReleaseD3DResources`、`CleanCache`、析构及 `CD3DResourceManager::DestroyResource` 调用链已闭环：bitmap、device 与 bitmap-source association 均为非拥有 identity；cached realizations 由 `CleanCache` 通过析构并原位重建缓存根节点释放。last-used color source 与 device-bitmap color source 是两个独立 owning 槽位，各自在写入时执行独立 AddRef；资源释放固定先清 cached realizations、再 Release last-used、最后 Release device-bitmap color source。即使两个槽位保存同一 identity，也必须按两个所有权各 Release 一次；manager 主动销毁后移出跟踪，后续 Dispose 幂等且全部成员保持释放后保护。现有生产实现无需修改；新增同一 identity 双槽位和 manager 主动销毁回归。验证：bitmap-cache lifetime 聚焦 12/12、全量主测试 3790/3790、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwD3DVertexBuffer::ReleaseD3DResources`、`CHwD3DIndexBuffer::ReleaseD3DResources` 及其 Lock/Unlock 直接调用链已闭环：动态 vertex/index buffer 创建后由 wrapper 持有底层 COM identity；正常释放前原生仅以 debug 断言要求未锁定，释放函数本身不调用额外 Unlock、不清理 locked 标志，只执行唯一一次 COM Release。Lock 成功但返回 null 或等于当前 byte offset 的无效地址时，原生尝试一次 Unlock并忽略其 HRESULT，清空输出后返回 driver-internal-error；正常消费链的 cleanup 则仅在已标记 locked 时尝试 Unlock并保持既有首错。现有生产实现无需修改；新增 locked 状态显式 Dispose与 manager 主动销毁回归，固定零额外 Unlock、vertex/index identity各释放一次、重复 Dispose幂等及释放后保护。验证：hardware buffer聚焦 60/60、全量主测试 3788/3788、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DTexture::ReleaseD3DResources`、`GetD3DSurfaceLevel`、`CD3DTextureSurface::Create/ReleaseD3DResources` 与 `CD3DResourceManager::DestroyResource` 的 texture/level-surface 独立 COM 所有权已闭环：texture wrapper 对 texture 持有独立引用；首次获取 level 时，D3D 返回临时 surface 引用，缓存的 texture-surface wrapper 再持有独立引用，调用方获得的 wrapper 也各自持有独立引用。texture 资源释放只释放 texture COM identity，不直接销毁缓存 wrapper；托管析构等价路径随后逆序 Dispose 缓存 wrapper。因调用方 level-surface 引用独立存在，texture 释放后该返回 wrapper 仍可访问，直至自身释放；manager 主动销毁则会分别失效并释放 texture、缓存 wrapper和调用方 wrapper，重复 Dispose 不重复 Release。现有生产实现无需修改；新增 texture 释放后外部 level surface 仍可访问，以及 manager 主动销毁后双方均受释放后保护且引用只释放一次的回归。验证：texture 聚焦 24/24、全量主测试 3786/3786、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DSurface::ReleaseD3DResources`、`CD3DDeviceLevel1::ReleaseUseOfRenderTarget`、`ReleaseUseOfDepthStencilSurface` 与 `CD3DResourceManager::DestroyResource` 的双 usage surface 释放链已闭环：surface usage 同时含 render-target 与 depth-stencil 时固定先通知 render-target、再通知 depth-stencil，两个通知均只清理设备的非拥有 identity，不持有或释放 surface COM 引用。render-target 清理在匹配 current identity 时先清 tracking、尝试 EndScene、切换 dummy，再释放 current depth tracking；因此共享 surface 的后续显式 depth-stencil 通知安全无操作，不会重复解绑或覆盖先前忽略的清理错误。全部通知完成后才执行唯一一次 surface COM Release，资源基类与管理器继续保证重复 Dispose/销毁不会重复释放。现有生产实现无需修改；新增共享双 usage surface 的精确原生调用顺序和单次 COM Release 回归。验证：render-target/depth-stencil 聚焦 59/59、全量主测试 3784/3784、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::SetDepthStencilSurface`、失败清理与 `ReleaseUseOfDepthStencilSurface` 直接调用链已闭环：非空绑定先开启 Z，null 绑定依次关闭 Z 与 Stencil；只有前置状态成功后，current-render-target 的非拥有 depth-stencil identity 才在底层 setter 前提交，因此底层绑定失败及后续清理失败都可能保留不准确 tracking。任何失败均依次尝试关闭 Z、关闭 Stencil和解绑 depth surface，三个清理 HRESULT 全部忽略，原首错继续经 `HandleDIE` 返回。release 对非空 surface 无条件下沉 state manager，不信任 current tracking；state manager 仅在已知 identity 匹配时解绑，而 device-level 仅在 tracking identity 匹配时清空 tracking。现有生产实现无需修改；新增 state-manager identity 与 current tracking 不一致时仍解绑匹配底层 surface 的回归。验证：depth-stencil 聚焦 32/32、全量主测试 3783/3783、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::IsDepthStencilSurfaceSmallerThan`、`ForceSetDepthStencilSurface`、`CD3DRenderState::SetDepthStencilSurfaceInternal` 与 `CD3DDeviceLevel1::Clear` 直接调用链已闭环：depth-stencil identity unknown 时为安全起见固定判定 smaller，known null 固定判定 not smaller，known non-null 才比较缓存 width/height；底层 setter 成功及非零成功后先提交尺寸，再按同一 HRESULT 提交 identity，失败只令 identity unknown，旧 identity/尺寸存储不主动擦除且查询不消费。`Clear` 无论 flags 是否含 depth/stencil，都在当前 surface 尺寸不足时先以 `(NULL,0,0)` 解绑；解绑失败作为首错返回并跳过底层 Clear，状态转 unknown 且下一轮重试，非零成功缓存 known null 并继续 Clear。托管失败分支已改为仅失效 identity，新增非零成功去重与失败跨轮重试回归。验证：depth-stencil/Clear 聚焦 88/88、全量主测试 3782/3782、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager` clip 写侧缓存与 `CD3DDeviceLevel1::SetClipRect/GetClipRect/SetRenderTarget` 直接调用链已闭环：clip rectangle 与 clip-set 标志是独立的 WPF 自有值状态；`SetClip` 只写 rectangle，`SetClipSet` 只写标志，`IsClipSet(rect)` 仅在标志为 true 时比较完整 rectangle。读取不调用 D3D getter、不持有 COM 引用；render-target 成功提交后先清 clip-set，再按 capability 更新 scissor cache、viewport 与 clipping matrix，底层失败前不提交 clip rectangle/标志。默认状态初始化同样只清 clip-set，不清旧 rectangle。托管 `ResetScissorAndClipCache` 已移除错误的 rectangle 清零，并新增标志清除与默认缓存重置仍保留 rectangle 的独立状态回归。验证：clip/render-target/default-state 聚焦 75/75、全量主测试 3780/3780、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::ReleaseUseOfDepthStencilBuffer`、`ForceSetDepthStencilSurface` 与 `CD3DDeviceLevel1::ReleaseUseOfDepthStencilSurface` 的非拥有 depth-stencil identity 释放边界已闭环：null 或不同 identity 直接返回 `S_OK` 且无设备调用；仅当 state-manager 当前已知 identity 与待释放 surface 相同时，以固定 `(NULL, 0, 0)` 解绑。成功及非零成功 HRESULT 原样传播并缓存 known null，失败 HRESULT 原样传播、缓存转 unknown 且允许跨轮重试；device-level 调用者忽略释放 HRESULT，并在匹配时清除 current-render-target depth-stencil identity。托管 `ReleaseUseOfDepthStencilBuffer` 现返回原始 HRESULT，并补齐 null、不同 identity、非零成功和失败重试回归。验证：depth-stencil 聚焦 30/30、全量主测试 3778/3778、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::DisableTextureStage` 的最大 texture blend stage 哨兵与普通缓存协议已闭环：允许 `stage == max`，该值直接返回 `S_OK` 且不调用设备；`stage < max` 时只通过普通 `SetTextureStageStateInline(stage, D3DTSS_COLOROP, D3DTOP_DISABLE)` 消费 texture-stage-state 缓存。成功及非零成功 HRESULT 原样传播并使匹配调用去重，失败 HRESULT 原样传播、状态保持未知并允许跨轮重试；`CD3DRenderState` 与 HW fixed-function pipeline 调用者继续按首错短路。现有生产实现无需修改；新增非零成功去重与失败重试两项设备级回归。验证：texture-stage 聚焦 50/50、全量主测试 3776/3776、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::ForceSetStreamSource` 与内联 `SetStreamSource` 的 stream 0 双状态缓存协议已闭环：普通入口仅在 vertex-buffer COM identity 与 stride 两项都已知且同时匹配时去重，null buffer 与任意 stride 仍作为独立组合匹配；强制入口固定通过 D3D9 vtable slot 100 提交 `(0, buffer, 0, stride)`，随后用同一 HRESULT 依次更新两张状态表。成功及非零成功使两项同时可见，失败使两项同时未知并允许跨轮重试，HRESULT 原样传播；强制入口即使匹配也重新提交。现有生产实现无需修改；新增强制入口匹配提交和失败双失效回归。验证：stream-source 聚焦 16/16、全量主测试 3774/3774、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetIndices` 与 `ForceSetIndices` 的 index-stream 状态协议已闭环：状态表虽按单项 COM identity（含 null）查询，但强制入口仅通过 D3D9 vtable 104 提交且刻意不调用 `m_indexStream.UpdateState`，因此缓存始终不可见；普通入口每次都重新提交相同或 null index buffer，以应对其他 D3D 调用隐式重置 index source。成功、非零成功与失败 HRESULT 均原样传播，任何结果都不阻止后续跨轮重试。托管实现已移除错误的成功后身份去重，并同步几何渲染精确调用计数。验证：索引状态聚焦 10/10、全量主测试 3772/3772、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetPixelShaderConstantB` 与 `ForceSetPixelShaderConstantB` 的单寄存器缓存协议已闭环：缓存按 register 独立并比较完整 `BOOL` 整数值，强制入口把值地址通过 D3D9 vtable 113 提交且 register count 固定为 1。成功及非零成功使该寄存器缓存可见，失败使该寄存器未知并允许后续普通调用跨轮重试，HRESULT 原样传播。`CD3DRenderState` 与 shader-effect 直接调用者继续消费普通缓存路径。现有生产实现无需修改；新增两项强制入口缓存回归。验证：pixel-shader bool constant 聚焦 15/15、全量主测试 3772/3772、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetPixelShaderConstantI` 与 `ForceSetPixelShaderConstantI` 的单寄存器缓存协议已闭环：缓存按 register 独立，仅以 int4 首元素判断匹配，但强制入口仍将完整四元素通过 D3D9 vtable 111 提交且 register count 固定为 1。成功及非零成功使该寄存器缓存可见，失败使该寄存器未知并允许后续普通调用跨轮重试，HRESULT 原样传播。`CD3DRenderState` 与 shader-effect 直接调用者继续消费普通缓存路径。现有生产实现无需修改；新增两项强制入口缓存回归。验证：pixel-shader int4 constant 聚焦 13/13、全量主测试 3770/3770、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetPixelShaderConstantF` 与 `ForceSetPixelShaderConstantF` 的区间缓存协议已闭环：普通入口逐寄存器比较，任一不匹配时强制提交整个区间，全匹配时去重；强制入口通过单次底层调用，并按同一 HRESULT 更新区间内每个寄存器。成功及非零成功使整段缓存可见，失败使整段未知并允许逐寄存器及跨轮重试，HRESULT 原样传播。`CD3DRenderState` 与 shader pipeline 调用者继续消费普通缓存路径。现有生产实现无需修改；新增两项强制整段缓存回归。验证：pixel-shader float constant 聚焦 12/12、全量主测试 3768/3768、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetVertexShaderConstantF` 与 `ForceSetVertexShaderConstantF` 的区间缓存协议已闭环：普通入口先按无符号 register range 相交规则失效 2D vertex-shader transform，再逐寄存器比较，任一不匹配时强制提交整个区间，全匹配时去重；强制入口按同一底层 HRESULT 更新区间内每个寄存器。成功及非零成功使整段缓存可见，失败使整段未知并允许跨轮重试，HRESULT 原样传播。2D transform 强制路径只在成功后记录 applied/start-register，3D transform 与 shader pipeline 普通路径继续消费缓存。现有生产实现无需修改；新增两项强制整段缓存回归。验证：vertex-shader constant/transform 聚焦 56/56、全量主测试 3766/3766、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetDefaultTexCoordIndices` 的默认恢复与标志边界已闭环：仅在默认标志为 false 时按 stage 升序通过普通状态设置恢复 `TEXCOORDINDEX = stage`，全部成功或非零成功后才置 true；只有强制设置非默认 texture-coordinate index 才预先失效标志，其他 state 或默认 index 即使提交失败也不失效。首错原样返回并跳过后续 stage，失败轮保持 false；此前成功 stage 的缓存使下一轮跳过已恢复项并仅重试失败项及后续项。现有生产实现无需修改；新增两项失效条件回归。验证：texture-stage-state 聚焦 35/35、全量主测试 3764/3764、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::SetDefaultState` 的尾部状态序列已闭环：在 transform/material 之后严格逐 stage 强制清空 texture，再强制清空 pixel shader、强制提交 stream 0、直接提交附加 streams `[1, MaxStreams)`、强制清空 indices，最后才重置 scissor rect 与 clip 缓存。所有调用按 `IFC` 首错原样返回并跳过后续组，非零成功继续；再次默认初始化会重新强制提交 texture、pixel shader、stream 0 与 indices，附加 stream 不污染 stream 0 缓存。现有生产实现与设备创建委托已精确匹配，无需修改。验证：相关聚焦 114/114、全量主测试 3762/3762、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::ForceSetWorldTransform`、`ForceSetNonWorldTransform` 与 `SetDefaultState` 的 transform/material 边界已闭环：默认状态严格按 world、view、projection、material 顺序提交；transform 按底层 HRESULT 更新独立缓存，失败项未知且首错跳过后续组，material 直接提交而不建立回读缓存。后续默认初始化必须重新强制提交全部三个 transform。托管设备创建路径已从可去重的 `SetTransform` 改为内部 `ForceSetTransform`，成功强制提交同时失效两类 2D transform 标志；新增 material 首轮失败后缓存可见与第二轮 transform 全量重提交、非零成功顺序回归。验证：相关聚焦 73/73、全量主测试 3760/3760、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::ForceSetRenderState`、`ForceSetTextureStageState` 与 `ForceSetSamplerState` 已核验为相同的底层提交后缓存更新协议：先恰好一次调用对应 D3D9 方法，再由 `CStateTable::UpdateState` 按 HRESULT 提交缓存；成功及非零成功把值标记为已知，失败把该项标记为未知而不恢复旧值，此前成功项保持可见。`SetDefaultState` 使用 `IFC` 返回首个失败并跳过后续状态组；后续再次初始化仍通过 `ForceSet*` 重新提交此前成功项和失败项。首次 `Init` 在持有基础设备引用并尝试获取独立 Ex 引用后初始化状态表，`Create` 失败通过对象析构释放部分缓存和 COM 所有权。现有生产实现已精确匹配，无需修改；新增 render-state 强制失败后未知/重试，以及默认状态首轮失败、部分缓存可见、后续组跳过和第二轮重新强制提交回归。验证：相关聚焦 97/97、全量主测试 3758/3758、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DRenderState::ResetState/Init` 与 `CHwRenderStateManager::Create/Init/SetDefaultState` 的 capability 消费边界已核验：`GetMaxStreams()` 在首次初始化和重置中原样传入，stream 0 单独处理，附加 stream 按 `[1, MaxStreams)` 消费；texture blend stage 只在 `CD3DRenderState::Init` 调用者处按 `MIL_TEXTURE_STAGE_COUNT`（8）截断，后续 texture stage、sampler 与 texture 清除均使用该截断值；各向异性沿用设备初始化的 `0 -> 1` 修正和 accessor 最大 4 限制。HW text 的四 stage 门槛只影响 text 初始化资格，不回写 capability。现有生产实现无需修改；新增 stream 0/1/典型值/`uint.MaxValue`、texture stage 0/低于/等于/高于上限/`uint.MaxValue` 与 text capability 保持原值回归。验证：default-state 与 capability 聚焦 222/222、全量主测试 3756/3756、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::GetMaxStreams/GetMaxTextureBlendStages/GetMaxSimultaneousTextures` 已再次核验为三个独立的内联只读数值 capability accessor：分别原样返回缓存 `D3DCAPS9` 对应字段，不执行范围修正、设备查询、device entry/use-context、COM 引用变化或状态提交。render-state 的 stream 消费直接使用 accessor，texture blend stage 的固定上限和 HW text 门槛属于调用者，mask 范围要求也仅为 debug `Assert`。现有生产实现无需修改；新增 HAL/REF/SW、基础/Ex identity 与重复读取组合回归，既有回归继续覆盖数值边界、字段隔离、无副作用及释放后保护。验证：capability accessor 聚焦 200/200、全量主测试 3747/3747、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::SupportsNonPow2Conditionally` 与 `SupportsNonPow2Unconditionally` 已重新核验为纯只读 `TextureCaps` accessor：conditional 仅在 `D3DPTEXTURECAPS_NONPOW2CONDITIONAL` 与 `D3DPTEXTURECAPS_POW2` 同时设置时成立，unconditional 仅在两个位同时未设置时成立。现有生产实现已精确匹配；新增回归覆盖无关位全开、HAL/REF/SW、基础/Ex identity、重复读取、不进入 device entry 和不触发 COM 引用变化，既有回归继续覆盖目标位四组合与释放后保护。验证：capability accessor 聚焦 181/181、全量主测试 3728/3728、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::CanAutoGenMipMap`、`CanStretchRectGenMipMap` 与 `CanStretchRectFromTextures` 已重新核验为纯只读 capability accessor：分别只判断缓存 `Caps2` 的 `D3DCAPS2_CANAUTOGENMIPMAP` 位、`StretchRectFilterCaps` 的 `D3DPTFILTERCAPS_MINFLINEAR` 位和 `DevCaps2` 的 `D3DDEVCAPS2_CAN_STRETCHRECT_FROM_TEXTURES` 位。三项不受 device type、其他 capability、基础/Ex identity、tier、managed pool、device entry 或 unusable 状态干扰，不调用底层 COM、不新增引用且重复读取无副作用；现有生产和回归已完整覆盖，释放后均在访问缓存前拒绝。验证：capability accessor 聚焦 175/175、全量主测试 3722/3722、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::UpdateSurface` 已重新核验为带 `HandleDIE` 的直接设备委托：释放后保护后通过 D3D9 vtable slot 30 与 Windows `Stdcall` 原样传递 system-memory source surface、可空 source RECT、default-pool destination surface和可空 destination POINT；不新增 COM 引用、临时资源、device entry/use-context或已标记 unusable 门控。成功、非零成功、普通失败与 device-lost HRESULT 原样返回，仅 driver-internal-error 记录设备不可用并转换为 display-state-invalid。现有生产与 14 个资源更新聚焦回归已完整覆盖，无需修改。验证：resource-update 聚焦 14/14、全量主测试 3722/3722、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::ColorFill` 已核验为纯直接设备委托：释放后保护后通过 D3D9 vtable slot 35 与 Windows `Stdcall` 原样传递 surface、可空 RECT 和 color，且不执行 `HandleDIE`、device-lost 映射或 unusable 门控。成功、非零成功、普通失败、device-lost 与 driver-internal-error HRESULT 均原样返回；现有生产和 7 个聚焦回归已完整覆盖，无需修改。验证：ColorFill 聚焦 7/7、全量主测试 3722/3722、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CHwRenderStateManager::IsDepthStencilSurfaceSmallerThan` 与 `CD3DDeviceLevel1::Clear` 的尺寸判定和解绑顺序已闭环：unknown、known null/non-null、相等尺寸和单维不足均只消费 state-manager 缓存，不调用 getter 或增加 COM 引用；尺寸不足时先解绑 depth-stencil，解绑失败保留首个 HRESULT并跳过底层 Clear。现有生产实现无需修改，并补充相等尺寸、单维不足和三类解绑失败回归。验证：聚焦 72/72、全量主测试 3722/3722、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::SetDepthStencilSurface`、`CD3DRenderState::SetDepthStencilSurfaceInternal` 与 `CHwRenderStateManager::ForceSetDepthStencilSurface` 的双层状态已核验：current depth-stencil 非拥有身份在底层绑定前提交，失败时允许保持不精确的新身份；state-manager identity 失败后转 unknown，尺寸只在成功后提交，原始 HRESULT 优先于关闭 Z/stencil 和解绑 cleanup 失败。现有生产实现无需修改。验证：depth-stencil 聚焦 67/67、全量主测试 3717/3717、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::TestRenderTargetFormat` 的临时对象所有权、HRESULT 首错与 cleanup 顺序已闭环：托管现保留 lockable secondary swap-chain 到统一 cleanup，不再在取得 back-buffer 后提前释放，并按原生 render-target surface、lockable texture、swap-chain 顺序确定性释放。测试状态缓存继续区分 capability failure 与不可缓存 OOM/video-memory OOM/driver-internal-error，首错传播保持不变。验证：格式测试 36/36、全量主测试 3717/3717、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::DestroyAllResources`、`DestroySomeActiveResources`、`DestroyListOfResources` 与最终 `DestroyResources(WithoutDelay)` 的 released/active 汇合顺序已重新核验：previous-frame delayed 先清理，四类 active 列表按 non-evictable、previous-frame evictable、current-frame not-in-use、current-frame in-use 顺序处理，最后 current-frame released 无延迟清空。回调重入经 pending destruction 汇合，新入队 released 资源由最终 flush 处理；重复通知和重复批量销毁幂等，各状态与统计只更新一次。生产实现无需修改。验证：resource-manager 聚焦 186/186、全量主测试 3717/3717、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CMILPoolResource::Release`、`CD3DResourceManager::UnusedNotification`、`DestroyResources` 与 `DestroyReleasedResourcesFromLastFrame` 的 released-resource 入队和帧分区已重新核验：零引用时资源仍为 valid、保留 manager identity、active-list 节点、use-context depth 与 current/peak 视频内存统计，`UnusedNotification` 只压入 current-frame released free-list；首次 flush 才按 LIFO 处理 current-frame 快照，要求延迟的资源迁入 previous-frame delayed list，其余资源执行 D3D 释放、tracking detach 与 current 视频内存扣减，peak 保留。previous-frame 清理先分离快照，回调新入队资源留在 current frame；托管以幂等集合避免测试可直接调用入口产生重复节点，现有重入与 current/previous 分区实现符合原生顺序，无生产修改。本轮新增重复通知下入队前后 identity、validity、active depth、计数和首次 flush 恰好一次释放的回归。验证：resource-manager 聚焦通过、全量主测试 3716/3716、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResource::Init` 与 `CD3DResourceManager::RegisterResource` 的初始化提交顺序已重新核验：资源 size/valid/evictable 与 manager identity 均在托管构造调用注册前可见；注册先完成 closed、identity、validity、重复注册、use-context 与 checked 视频内存加法校验，再提交总集合、current/peak 统计和 evictable 首次 `Use` 状态。原生 `Use` 为无失败返回的列表状态操作，不存在可翻译的回调失败协议；托管所有可失败校验均位于集合、统计与 active-depth 变化前，构造失败不会遗留 manager tracking、计数或半初始化 manager identity。现有生产和回归已完整覆盖，无代码修改。验证：resource-manager 聚焦通过、全量主测试 3715/3715、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::UnusableNotification`、`DestroyResource`、`DestroyReleasedResourcesFromLastFrame` 与 device `DestroyAllResources` 链已闭合：invalid resource 先释放自身 D3D 资源并从 active/current/previous/released tracking 拆除，再清理上一帧 delayed-released 快照；清理快照前先置空 previous-frame 队列，因此回调新入队的 current-frame released 资源不会被本轮误删。托管原有直接 invalid、重复 Dispose、use-context 深度保持、current/previous-frame 分区、device-lost 与最终 device Dispose 恰好一次释放均已有回归；本轮修复回调重入时 nested invalid resource 因 pending 队列尚未实际拆除而跳过上一帧清理的缺口，新增延后清理标记，使 pending invalid resource 真正拆除后再处理 previous-frame 队列，并继续排空清理回调产生的新 pending 销毁。验证：resource-manager 聚焦通过、全量主测试 3715/3715、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceManager::UnusableNotification`、`NotifyDeviceLost` 与最终 `UnusedNotification` tracking 收口已闭合：首次 unusable 在 manager 临界区内先以 per-device lost 标记保证 adapter-invalid 只通知一次，再从 usable 分区移动到 unusable 分区；重复 unusable、已因 display change 位于 unusable 分区、独立软件设备及初始化中未跟踪设备不重复通知或创建分区条目。最终释放跟踪设备时同样先执行幂等 lost 通知，再完成设备资源销毁和 tracking 删除；托管新增独立 lost 集合，对齐原生 `fIsDeviceLost`，并修复 unusable/dispose 路径原先在分区移动或删除后才通知的先后差异。管理器释放同步清空 lost 状态，现有 display-set 引用清理保持不变。验证：device-manager/device-state/gpu-marker/resource-manager 聚焦 326/326、全量主测试 3714/3714、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceManager::CreateNewDevice` 的 identity 转移与失败清理边界已闭合：Ex 创建成功后 QueryInterface 基础 `IDirect3DDevice9` 并立即释放临时 Ex identity；wrapper 初始化、render-target 格式检查或 tracking list 预留失败时销毁未跟踪 wrapper，最后释放基础设备临时引用；成功时 wrapper 创建引用直接转移给调用方，manager 列表不额外持有 COM 引用。adapter-valid 通知位于底层创建成功后、wrapper 初始化和列表插入前，后续失败不产生 manager adapter-invalid 通知。托管修复 Ex→基础 identity 查询失败被折叠为 invalid-call 的差异，现在保留 QueryInterface 原始 HRESULT，并各释放一次失败返回接口与 Ex 临时引用。验证：identity-query 聚焦 11/11、device-manager 聚焦 111/111、全量主测试 3712/3712、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceManager::GetD3DDeviceAndPresentParams` 的 display-state change 返回与释放边界已闭合：输出设备先清空，display mode 获取后先复用并取得外部引用，再组合本次请求的 present parameters，复用失败才创建并跟踪新设备；Cleanup 检测到 display change 时无条件释放已取得的输出引用，并以 display-invalid 覆盖此前成功或失败。复用设备仅释放本次外部引用并保留 manager 缓存，新建设备释放唯一外部引用后从 manager 移除。托管现有 finally 与新建/复用清理分支已覆盖，无生产修改；新增原创建失败被 Cleanup display-invalid 覆盖且不遗留跟踪设备的回归。验证：device-manager 聚焦测试 111/111、全量主测试 3712/3712、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceManager::FindDeviceMatch` 的设备复用边界已闭合：type 必须相同；SW 设备仅按 type 共享，HAL 设备比较 adapter ordinal 和排除 `D3DCREATE_DISABLE_DRIVER_MANAGEMENT_EX` 后的 behavior flags；focus window 明确不参与匹配。命中后原生 AddRef 返回设备并回写实际 behavior flags，present parameters 仍按本次请求重新组合。托管现有实现已完整覆盖，无生产修改；新增 HAL 不同 focus 仍复用且返回请求窗口 present parameters 的回归，并强化 SW 跨 adapter/flags 复用后的实际 flags 回写。未扩展设备缓存、公开 API、swap-chain/window focus、线程、终结器、AV、InteropDeviceBitmap、UCE、生产 ABI 或 `Reset/ResetEx`。验证：device-manager 聚焦测试 110/110、全量主测试 3711/3711、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `IDirect3DDevice9::GetCreationParameters` 的缓存与消费边界已闭合：内部 render-target 格式测试只消费实际 adapter ordinal，用户设备 InteropDeviceBitmap 只消费用户设备 adapter ordinal，AV mixer 只在 Debug 断言 multithreaded behavior flag；托管实际创建链缓存 adapter ordinal、device type、最终 behavior flags 与实际 focus window，并保持释放后保护。`Direct3D9DeviceManager.RecreateDevice` 已修复非零 focus window 遗漏，重建时现在与 present parameters、adapter LUID 等创建状态一起保留，且在释放旧设备前读取缓存；未新增直接 COM 包装或扩展 InteropDeviceBitmap、AV、生产 ABI、UCE、线程、终结器及 `Reset/ResetEx`。验证：device-manager/capability-accessor 聚焦测试 284/284、全量主测试 3710/3710、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::SetRenderTargetForEffectPipeline` 已完成可达性与窄状态差异核对：原生调用者仅位于 `BlurEffect.cpp` 的 effect pipeline；当前四项目托管生产树没有该方法、effect pipeline 调用者或等价可达路径，因此按禁止扩展边界不创建推测性 API，也不把普通 `SetRenderTarget` 改造成 effect 入口。原生契约记录为：same identity 直接成功且不结束或重开 scene；已有 scene 的 EndScene 失败时保持旧 identity，不调用 native SetRenderTarget 或 rollback；native SetRenderTarget 成功后才提交 current identity 并只 invalidates clip，不设置 viewport、surface-to-clipping matrix、target-surface 或 scissor；BeginScene 失败后通过 `ReleaseUseOfRenderTarget` 清空 identity，并忽略 cleanup HRESULT；外层最后恰好一次执行 `HandleDIE`。普通 `SetRenderTarget` 现有可达路径及既有回归保持不变。验证：全量主测试 3706/3706、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::SetRenderTarget` 失败 Cleanup 与 `ReleaseUseOfRenderTarget` 的首错保持边界已闭合：native SetRenderTarget 成功后才提交 current identity；viewport、surface-to-clipping matrix 或 BeginScene 任一步失败均先清空 current identity，再按 scene 状态尝试 EndScene、把 slot 0 绑定到借用 dummy back-buffer，并释放当前 render-target 关联的 depth-stencil 使用。Cleanup 内 EndScene、dummy SetRenderTarget 和 depth-stencil 清理 HRESULT 全部忽略，不阻断后续清理，也不得通过内部 `HandleDIE` 污染外层首错；托管实现为此拆出不执行错误映射的 EndScene 清理路径。外层原始 driver-internal-error 仍只在 SetRenderTarget 返回边界映射为 display-invalid 并记录原始 unusable reason；普通失败和 device-lost 保持原值。native SetRenderTarget 自身失败时 current identity 尚未提交，不执行解绑或 dummy 回退。未扩展 scene 恢复、render-target 所有权、dummy 引用、公开 API、生产 ABI、线程、终结器、UCE/资源协议或 `Reset/ResetEx`。验证：render-target-state 聚焦测试 27/27、全量主测试 3706/3706、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::ReleaseUseOfRenderTarget` 与 `CD3DSurface::ReleaseD3DResources` 的当前 render-target 非拥有 identity 解绑边界已闭合：surface 释放通知仅在 identity 与 current target 相同时执行清理，非当前 render-target 直接进入自身 COM Release 且不改变 scene、dummy、depth-stencil 或设备状态。当前 identity 先清空，再在 scene 内调用 EndScene，随后把 slot 0 绑定到借用 dummy back-buffer，再释放当前 render-target 关联的 depth-stencil 使用，最后才由 surface 释放自身 COM 引用；EndScene、dummy SetRenderTarget 和 depth-stencil 清理失败均被忽略，不阻断后续清理、不标记设备不可用，也不产生新返回值。EndScene 失败沿用原生状态语义保留 in-scene 标志，但解绑后不自动 BeginScene。现有生产实现无需修改；新增严格非当前无副作用及当前清理失败顺序回归。未扩展普通 scene 生命周期、render-target 所有权、dummy 解绑策略、present 协议、公开 API、生产 ABI 或 `Reset/ResetEx`。验证：render-target-state 聚焦测试 25/25、全量主测试 3704/3704、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1` 构造、`Init`、失败 Cleanup 和析构对基础 `m_pD3DDevice` 与可选 `m_pD3DDeviceEx` 的接口所有权边界已闭合：基础接口由设备创建结果或显式 AddRef 形成独立所有权，Ex 接口由 QueryInterface 形成另一份独立所有权；初始化失败按临时资源、Ex、资源管理器和基础接口的逆序清理，并保留首个失败 HRESULT。普通设备析构只释放基础引用，Ex 设备先释放 Ex 引用再释放基础引用；即使基础与 Ex 接口 identity 地址相同，也按两份接口引用各释放一次而不按地址去重。托管 `Direct3D9Device` 构造与 `Dispose` 已符合该顺序，重复 `Dispose` 无副作用，释放后接口访问抛出 `ObjectDisposedException`。生产实现无需修改；未扩展 `Reset/ResetEx`、设备重建、线程、终结器、公开 API 或生产 ABI。验证：接口所有权与初始化失败聚焦测试 15/15、全量主测试 3702/3702、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DDeviceLevel1::InitializeIMediaDeviceConsumer()`、AV `MediaDeviceConsumer` 与 `GetUnderlyingDevice()` 的窄设备身份和 COM 引用所有权边界已闭合：Level1 对普通和 Ex 设备都只把当前基础 `IDirect3DDevice9` 借用 identity 同步交给 consumer，不执行 QueryInterface、包装、device entry、use-context 或设备侧 AddRef/Release。临时 consumer 按原生 `ReplaceInterface` 先释放旧引用再保存并 AddRef 新引用，重复初始化保持每次引用变化配对；`GetIDirect3DDevice9` 按 `SetInterface` 返回独立引用，调用者释放该引用后，consumer 析构只释放自身持有引用。null consumer 和已释放 device 保持现有异常边界，consumer 回调异常原样传播且不改变设备作用域或引用。生产实现无需修改；未扩展公开 API、生产 ABI、AV/UCE/资源协议、媒体呈现、线程、终结器或跨线程接管。验证：media-device-consumer 聚焦测试 7/7、全量主测试 3698/3698、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResource::Init()` 与 `CD3DResourceManager::RegisterResource()` 对应的托管构造注册边界已闭合：托管资源在调用 manager 前已建立 manager identity、validity、evictable 和 size；成功注册原子地加入总集合、更新 current/peak 视频内存，并在 active use-context 内将 evictable 资源登记为当前深度使用。closed manager、context 外 evictable、重复注册、外来 manager、invalid 资源和视频内存加法溢出均在集合、统计和 use-context 变化前拒绝；构造注册失败不会进入任何 manager 集合。视频内存加法改为 checked，防止托管无符号回绕破坏统计不变量。未新增工厂、两阶段初始化、析构回滚通知、线程保护、引用计数、终结器、公开 API、生产 ABI、异常种类、HRESULT、COM 或资源协议。验证：资源管理器聚焦测试 183/183、全量主测试 3695/3695、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResource::DestroyAndRelease()`、`CD3DResourceManager::UnusableNotification()` 与 `DestroyResource()` 对应的托管确定性释放顺序已闭合：首次 `Dispose` 先将资源置 invalid，再通知 manager；释放回调期间资源仍关联原 manager/device，集合计数和视频内存统计尚未更新，回调后才解除管理并恰好一次移出集合、减计和扣减统计。已 invalid 但仍受管理的资源不由 `Dispose` 重复通知，已解除管理和重复 `Dispose` 不重复释放；evictable 资源处于 previous-frame、current-frame not-in-use 和 active use-context 时均按相同顺序释放，且不改变 use-context 配对。现有生产实现无需修改；未引入原生 `m_cRef` 引用计数、线程保护、终结器、跨线程接管、公开 API、生产 ABI、异常、HRESULT、COM 或资源协议。验证：新增聚焦测试 4/4、资源管理器聚焦测试 179/179、全量主测试 3691/3691、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DUseContextGuard` 构造/析构、`CD3DDeviceLevel1::EnterUseContext()`/`ExitUseContext()` 转发及代表性 `CHwDeviceBitmapColorSource::CopyPixels` 调用作用域已闭合：现有托管 guard 构造恰好进入一次并保存深度，`Dispose` 恰好按保存深度退出一次且重复释放无副作用；真实像素复制调用链的正常成功、非零成功、guard 内早退和普通 HRESULT 失败均恢复进入前 depth，清理不改写返回的首个结果。已有外层 context 时只建立并清理内层深度；内层新资源退出后迁移到 not-in-use，重复使用的外层资源保留原 active depth、identity、validity 和 management ownership。生产实现无需修改；未扩展普通 scene/render-target 生命周期、线程、公开 API、生产 ABI、异常、HRESULT、COM 或资源协议。验证：像素复制聚焦测试 6/6、资源管理器聚焦测试 176/176、全量主测试 3688/3688、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::EnterUseContext()`、`ExitUseContext()` 与 `Use()` 的 current-frame in-use 栈不变量已闭合：嵌套深度严格递增并反向配对，首次使用保存首次深度和尾部位置，同帧更深 context 重复使用无移动或深度改写；退出只迁移当前深度连续尾段并保持外层资源与稳定顺序，错误深度无状态副作用。后续 `EndFrame` 和 previous/current LRU/MRU 端点保持原生顺序。现有生产实现无需修改；未扩展线程、公开 API、生产 ABI、异常、HRESULT、COM 或资源协议。验证：资源管理器聚焦测试 176/176、全量主测试 3685/3685、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager` 构造、`Init()`、析构泄漏断言及 `CD3DDeviceLevel1` 拥有者关闭顺序对应的托管初始化/关闭不变量已闭合：新 manager 的活动、released、delayed、pending、use-context、current total、peak 和资源计数均从零开始，带 device 构造只保存稳定借用身份且重复读取无副作用。`Close()` 仅在所有资源集合、pending destruction、销毁作用域、use context 与 current total 全部清空时成功；失败无状态副作用。成功关闭保留历史 peak 和借用 device 身份，重复关闭无副作用，关闭后注册继续拒绝；device Dispose 先批量销毁资源再关闭 manager，并保持相同不变量。未引入终结器、跨线程接管、线程保护、公开 API、生产 ABI、新异常种类、HRESULT、COM 或资源协议。验证：资源管理器聚焦测试 172/172、全量主测试 3681/3681、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::AddToVideoMemoryUsage()`、`SubtractFromVideoMemoryUsage()` 与 `m_peakVMConsumption` 对应的托管高水位统计边界已闭合：程序集内部 `PeakVideoMemoryConsumption` 仅在成功注册增加 current total 后单调更新；零大小、重复注册、外来 manager 和注册失败不改变峰值。主动 Dispose、eviction、released/delayed/pending、批量销毁和设备关闭只降低 `TotalVideoMemoryConsumption` 而不回退 peak，后续更高总量才形成新峰值；下溢校验在任何释放、移表或解除管理副作用前失败，current、peak、资源数、释放次数和资源身份均保持不变。未实现 media-control/perf meter、原子跨线程统计，未扩展公开 API、生产 ABI、异常种类、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 168/168、全量主测试 3677/3677、ABI 8/8，Debug 解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::DestroyListOfReleasedResources()` 与 `DestroyResources()` 对应的托管 released free-list 批量清理边界已闭合：当前帧 `_releasedResources` flush 后严格按 SLIST 后进先出方向遍历，`WithDelay` 只把 `RequiresDelayedRelease` 资源压入 delayed 链，形成下一帧再次反转的稳定顺序；`WithoutDelay` 全部立即清理，上一帧 delayed 链按现有链向遍历。每个 flushed 批次处于统一销毁作用域，集合资源释放引发的嵌套销毁只进入 pending destruction 队列并在整个批次结束后排空，重复嵌套请求不重复入队；新产生的 released 资源保留给下一次 current-frame flush。每个资源仅释放、解除管理、减计和扣减视频内存一次。并发 SLIST、引用计数等待、`Sleep`、轮询和跨线程接管明确排除；未扩展公开 API、生产 ABI、异常、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 159/159、全量主测试 3668/3668、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::DestroySomeActiveResources()` 与 `DestroyListOfResources()` 对应的托管批量活动资源销毁边界已闭合：`DestroyAllResources` 严格按 non-evictable、previous-frame evictable、current-frame not-in-use、current-frame in-use 四类活动列表顺序处理；资源先 invalid，在仍关联 manager 时执行释放回调，回调后解除管理并恰好一次移出列表、减计和扣减视频内存。批量释放中的嵌套销毁延后到四类遍历完成后由既有 pending destruction 队列统一处理，released、delayed-released 与 pending 资源不重复释放。原生并发 free-list 引用等待、`Sleep`、轮询和跨线程接管明确排除；未扩展公开 API、生产 ABI、异常、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 157/157、全量主测试 3666/3666、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- 原生 `CD3DResourceManager::GetUnusedResourceFromList()`、`FindLRUResourceInAPreviousFrame()` 与 `FindMRUResourceInCurrentFrame()` 对应的托管候选选择边界已闭合：现有 private helper 只观察列表端点，previous-frame 选择链首最旧资源，current-frame not-in-use 选择链尾最新资源，且 previous 始终优先；空集合及 released/delayed-released、无效、解除管理或 active-depth 非零资源不产生候选。生产实现无需修改；新增 previous 优先、复用迁移、released 清理后 current MRU 和 active 无候选无副作用回归。未扩展公开 API、生产 ABI、异常、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 152/152、全量主测试 3661/3661、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- 原生 debug `CD3DResourceManager::m_cDbgResources` 对应的托管注册/销毁计数一致性已闭合：现有 internal `ResourceCount` 只统计成功注册且尚未完成释放、解除 manager 与移出注册集合的资源；成功注册恰好加一，主动 Dispose、manager eviction、released/delayed-released 最终销毁、嵌套及批量销毁和设备关闭均恰好减一，重复销毁不重复减计或下溢。注册现拒绝重复资源及外来 manager 资源，失败不改变双方计数、视频内存统计、validity 或 management identity。未扩展公开 API、生产 ABI、异常种类、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 148/148、全量主测试 3657/3657、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- 原生 debug `CD3DResourceManager::DbgAssertPrevFrameListSorted()` 对应的托管 previous-frame evictable 排序不变量已闭合：资源以程序集内部 `LastUsedFrame` 记录每次 `Use()` 所在帧，internal 纯查询 `IsPreviousFrameListSorted()` 确认 previous-frame 活动资源按最后使用 frame 从旧到新排列、同帧稳定 MRU 且 active depth 为零，并排除 released/delayed-released 非活动项。覆盖跨多帧追加、旧资源复用迁出、LRU eviction、Dispose、批量销毁、设备关闭及重复无副作用查询；未扩展公开 API、生产 ABI、异常、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 140/140、全量主测试 3649/3649、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResourceManager::DbgResourceIsActive()` 对应的托管单资源活动成员查询边界已闭合：内部 `IsResourceActive` 只判断资源是否属于 non-evictable、previous-frame evictable、current-frame not-in-use 或 current-frame in-use 四类活动集合，并统一排除 released、delayed-released、无效及已解除管理资源。覆盖同一/外来 manager、四类集合、frame 迁移、Dispose、eviction、pending nested destruction、批量销毁、设备关闭及重复无副作用查询；未扩展公开 API、生产 ABI、异常、HRESULT、线程、COM 或资源协议。验证：资源管理器聚焦测试 131/131、全量主测试 3640/3640、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResourceManager::AreActiveResources()` 对应的托管活动资源聚合边界已闭合：查询只观察 non-evictable、previous-frame evictable、current-frame not-in-use 与 current-frame in-use 四类活动集合，并统一排除 released、delayed-released、无效、已解除管理和待嵌套销毁资源。空 manager、四类列表、混合集合、主动 Dispose、单资源 eviction、frame 推进、批量及嵌套销毁、设备关闭和重复无副作用查询均已覆盖；未扩展公开 API、线程、HRESULT、COM 或资源协议。验证：资源管理器聚焦测试 117/117、全量主测试 3626/3626、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResource::Manager()`、`Device()` 与 debug `DbgIsAssociatedWithDevice` 对应的托管非拥有身份查询边界已闭合：device 构造时建立内嵌 manager 的所属 device 身份，resource 在仍受管理时重复返回同一 manager/device 实例，不创建包装、不复制、不增加 COM 或托管所有权，也不进入 device entry/use-context、不调用 D3D COM、不改变资源状态、frame、计数、统计或释放次数。原生 `DestroyResource` 的顺序确认释放回调期间身份仍可达，回调后解除 manager；主动 Dispose、eviction、批量销毁和设备关闭后均不可再查询。debug helper 未扩展为生产或公开契约。验证：资源管理器聚焦测试 110/110、全量主测试 3619/3619、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResource::GetResourceSize()`、`CD3DResourceManager::AddToVideoMemoryUsage` 与 `SubtractFromVideoMemoryUsage` 的内部统计边界已闭合：资源构造缓存只读 `uint` 大小，零大小合法；manager 在注册成功时单次加计，在释放、解除管理并移出注册集合后单次减计，重复 Dispose、manager eviction、批量及嵌套销毁均不重复减计，减计拒绝下溢，关闭后创建失败无统计遗留。纹理仅在全部 level 描述成功且格式受支持后按每级宽、高和原生格式字节数累计估算大小；失败保持空输出、零注册与零统计。未扩展 peak/media-control/perf meter、并发或公开协议。验证：资源管理器聚焦测试 104/104、全量主测试 3613/3613、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResource::IsValid()` 与 `IsEvictable()` 的纯状态读取边界已闭合：两个访问器只返回资源自身缓存标志；validity 仅由初始化、主动 Dispose/不可用通知和 manager invalidation 写入，evictable 仅由构造初值与 `SetAsEvictable` 单向写入。初始、首次及重复转换、无/单层/嵌套 context、manager eviction、Dispose 与 manager destruction 前后重复读取均不进入 device entry/use-context，不调用 manager 或 D3D COM，也不改变 active depth、MRU/eviction 顺序、frame、资源数、释放计数、management identity 或所有权。现有生产实现无需修改。验证：资源管理器聚焦测试 99/99、全量主测试 3608/3608、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DResource::SetAsEvictable` 的状态转换与 MRU 定位边界已闭合：有效 managed 资源只设置 evictable 标志；已有 use-context 时在当前深度记录 use，外层已持有时保持原 active depth；无 context 时严格临时 Enter/Use/Exit，返回后完全退出并进入当前帧 not-in-use 队列。首次与重复转换、嵌套 context、无效/已释放拒绝、状态与计数保持、当前帧 MRU eviction 顺序均已覆盖。现有生产实现无需修改。验证：资源管理器聚焦测试 94/94、资源管理器与设备 entry 聚焦测试 122/122、全量主测试 3603/3603、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DDeviceLevel1::IsInAUseContext` 的纯状态转发边界已闭合：设备只返回内嵌 resource manager 当前 use-context 深度是否大于零，不进入或退出 use-context/device entry，不暴露深度，不调用 D3D COM，也不改变资源注册、active depth、frame、scene、render-target 或资源所有权；无 context、单层、嵌套和逐层退出后的布尔状态均与 manager 一致，重复查询无副作用，设备释放后在读取状态前拒绝。现有生产实现无需修改。验证：资源管理器与设备 entry 聚焦测试 117/117、全量主测试 3598/3598、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DDeviceLevel1::Use` 的窄转发边界已闭合：设备只把既有资源使用记录转发给内嵌 resource manager，不创建、注册、释放或复制资源，不自行进入/退出 device entry 或 use-context，不调用 D3D COM，也不改变 frame、scene、render-target 或资源所有权；有效 evictable 资源只在已有 use-context 中记录当前深度，重复使用保持外层深度，null、外来 manager、无效资源、缺少 use-context 与设备释放后均按既有首错语义拒绝。验证：资源管理器与设备 entry 聚焦测试 115/115、全量主测试 3596/3596、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DDeviceLevel1::GetResourceManager` 的非拥有身份访问边界已闭合：重复访问返回设备内嵌的同一 manager 实例，不创建包装、不复制、不关闭、不增加资源或 COM 引用，不进入 device entry/use-context，也不改变 resource/frame/released 状态；设备继续独占 manager 的销毁与关闭顺序，释放后在暴露状态前拒绝访问。验证：资源管理器与设备 entry 聚焦测试 109/109、全量主测试 3590/3590、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DDeviceLevel1::GetClipRect` 的纯缓存读取边界已闭合：clip 已设置时直接返回已提交交集缓存，未设置时只按最近成功绑定 target surface 的最新尺寸返回 `(0, 0, Width, Height)`；重复读取稳定，target 切换后未设置结果随描述更新，已设置结果不重新求交。读取不调用 D3D COM、不进入或退出 device entry/use-context，也不改变 scissor、viewport、matrix、scene、current render-target、clip-set、缓存或资源引用；释放后在状态读取前拒绝。验证：clip state 聚焦测试 19/19、全量主测试 3585/3585、ABI 8/8，Debug 四项目及解决方案构建 0 警告、0 错误。

- `CD3DDeviceLevel1::CleanupFreedResources` 与软件 fallback 转发的两阶段立即清理边界已闭合：严格先销毁上一帧 released 资源，再以 `WithoutDelay` 逆序销毁当前 released 资源；delayed-release 资源在该入口立即销毁，active/in-use 资源保持有效，不调用 `EndFrame` 或推进 frame。重复清理保持单次释放；软件 surface 无设备时静默跳过，有设备时 entry 仅包围清理，释放回调可观察 entered 状态，嵌套调用恢复外层 entry，设备释放后在资源操作前拒绝。验证：资源管理器与软件 3D surface 聚焦测试 116/116、全量主测试 3582/3582、ABI 8/8，Debug 四项目构建 0 警告、0 错误。

- `CD3DDeviceLevel1::CreateLockableTexture` 的直接设备、wrapper 所有权与失败清理边界已闭合：autogen/non-autogen levels、managed/system-memory 参数、null shared-handle、成功 AddRef/资源注册和临时引用释放均符合原生顺序；底层失败或 wrapper 的 level count/`GetLevelDesc` 失败保持首错、空输出、零遗留注册和单次 COM 释放，driver-internal-error、已处理 device-lost 后仍调用及释放后保护保持既有语义。验证：texture creation 聚焦测试 17/17、全量主测试 3577/3577、ABI 8/8，Debug 四项目构建 0 警告、0 错误。

- `CD3DDeviceLevel1::GetSupportedAnistotropicFilterMode` 的 capability 缓存边界已闭合：初始化仅依据 magnification/minification anisotropic 位选择 full anisotropic、min-only anisotropic 或 linear fallback；访问器只读缓存，不重新探测 capability，不进入 device entry，不调用 COM，也不改变 sampler/render state 或资源引用。验证：capability accessor 聚焦测试 171/171、全量主测试 3576/3576、ABI 8/8，Debug 四项目构建 0 警告、0 错误。

- `CD3DDeviceLevel1` 的 device-owned 动态 index/vertex buffer 析构顺序已闭合：设备关闭先按 index、vertex 顺序释放自身持有引用，再由资源管理器批量销毁底层 D3D buffer，最后释放底层 device COM；部分初始化、重复 buffer 释放与重复关闭均保持单次释放。验证：dynamic buffer 聚焦测试 15/15、全量主测试 3575/3575、ABI 8/8，Debug 四项目构建 0 警告、0 错误。

- `CD3DDeviceLevel1` 的析构 entry 配对与 current-RT depth-stencil 空状态前置已闭合：托管 `Dispose` 在任何资源或 COM 释放前要求外部 entry 计数与线程身份同时为空；空状态后由关闭路径自身 Enter。批量销毁 owning depth-stencil surface 时先通知设备按既有 state-manager 路径解绑并清空 current-RT 非拥有身份，再释放 surface COM；资源销毁返回后再次明确提交空身份，随后才关闭资源管理器并释放底层 device COM。render-target 与 depth-stencil 同属一个资源、解绑失败、重复资源释放和重复关闭均不遗留身份或 double-release。验证：depth-stencil 聚焦测试 28/28、全量主测试 3573/3573、ABI 8/8，Debug 四项目构建 0 警告、0 错误。
- `m_pCurrentRenderTargetNoRef` 是仅用于身份比较和释放通知的非拥有指针：两个 `SetRenderTarget*` 只在底层 SetRenderTarget 成功后提交它；相同 surface 直接返回；后续 viewport、matrix 或 BeginScene 失败均进入统一解绑清理。
- `m_fInScene` 只在底层 BeginScene/EndScene 成功后改变。正常 render-target 切换先按需 EndScene，成功绑定后 BeginScene；释放当前 surface 时清空身份、按需 EndScene、借用 dummy 解绑且不重新 BeginScene；present 失败则明确跳过 EndScene，直接清空身份、借用 dummy 并释放 depth-stencil 使用。
- Owning `CD3DSurface` 在释放 D3D 资源前按 usage 通知设备；设备析构先清空当前 render-target 身份，再销毁资源，因此析构期间 surface 通知不会访问或解绑已不再关心的目标。托管 `Direct3D9Surface` 已有等价 owning-wrapper 通知、resource manager 顺序和 disposed guard，`Direct3D9Device` 也已有成功提交式 `_inScene`。
- 设备保护边界已确认：仅 software device 使用 entry lock；`IsEnsuringCorrectMultithreadedRendering` 只反映该 entry critical section 是否初始化，并具备释放后保护；嵌套计数与线程身份按 Enter/Leave 原生顺序维护，最终 Leave 才清除身份；`IsProtected` 精确结合 multithreaded behavior flag、force confirmation 和当前线程，`IsEntered` 只反映 entry count。基础/Ex identity 与 managed-pool 提交也已闭合：Ex 探测失败仅降级并释放临时引用，两个 accessor 只读缓存。`HwCaps::CheckDeviceLevel1` 现已确认只拒绝 POW2 且无 conditional-NPOT、square-only、少于两个 texture stage/texture、非 SW 缺少 color-write mask 以及必需 source/destination blend 位，统一返回 `E_FAIL`；texture-op、shade、raster、纹理尺寸和 shader version 不属于该 helper 的拒绝条件。`GatherSupportedTextureFormats` 现已确认固定探测七种 D3D texture format，全部调用使用 adapter/device/display-format、usage 0 与 texture resource type；非负 HRESULT 视为支持，失败仅按原生 alpha/非 alpha 精度链回退，全部失败保持 `Undefined`，SW/REF/HAL 无额外分支。resource cache index 生命周期也已闭合：32 槽全局位图保留 software index 0，真实设备获取失败保持无效 token 并继续初始化，成功索引只转移唯一所有权，部分初始化失败与正常析构均归还；无效 token 的 HW 消费者保持拒绝或 software fallback。`MarkUnusable` 已闭合首次 driver-internal-error 通知、display-invalid 统一提交、跨线程 deferred 清理及单次执行；设备级 use-context 薄包装也已闭合嵌套深度透传、逆序退出、状态观察和释放后保护。swap-chain wrapper 现还按原生在 base AddRef 后探测并独立持有可选 `IDirect3DSwapChain9Ex`，探测失败但返回指针会立即释放且不改变创建 HRESULT，资源释放保持 base、Ex、cached back buffers 顺序；初始化后的 `GetBackBuffer` 只对 cached surface wrapper 单次 AddRef，越界返回 `E_INVALIDARG`，不重复调用 native slot 5，不产生 use-context/resource-manager 副作用，释放后在 COM 前拒绝。`ReleaseD3DResources` 还已确认由管理器在资源先失效后回调，按 base、Ex、cached wrappers 顺序释放并清空；重复释放、部分初始化失败和批量销毁无 double-release 或遗留注册，且不改变 use-context。资源管理器嵌套销毁边界也已闭合：资源 release 回调中的其他资源销毁请求延迟到当前资源完成 release、detach 和 remove 后处理，保持原生禁止嵌套 `DestroyResource` 的列表完整性约束；批量销毁在回调引起列表变化时无跳项、重复释放或遗留注册。released free-list 延迟语义现也已闭合：本帧队列按 LIFO flush，`WithDelay` 仅迁移要求延迟的资源并反转迁移顺序，`WithoutDelay` 立即销毁；上一帧链在处理前摘空并无条件销毁，回调新入队项留待后续 flush，精确返回实际销毁计数。`FreeSomeVideoMemory` 已闭合 released-resource 前三段优先路径及其后的单资源 active eviction：仅 D3D OOVM 或 SW 设备的 `E_OUTOFMEMORY` 进入，按上一帧、本帧 `WithDelay`、新形成上一帧的顺序逐段尝试，任一段实际销毁后立即短路；三段均未销毁时先取 previous-frames 头部 LRU，缺失才取 current-frame-not-in-use 尾部 MRU，并只失效、释放、detach/remove 一个有效可驱逐资源。迁移但未销毁不算成功，回调新增项保持后续处理；未实现并发 refcount/Sleep 分支。`DestroyAllResources` 现也按原生先销毁上一帧 released 链，再依次清空 non-evictable、previous-frames、current-frame-not-in-use、current-frame-in-use 四类 active 列表，最后以 `WithoutDelay` flush 本帧 released 链；列表快照承受释放回调引起的嵌套销毁或新入队，保持先失效后释放、detach/remove、无重复释放或遗留注册及 use-context 深度不变。`EndFrame` 的非调试迁移语义也已闭合：仅在 use-context 已退出且 current-frame-in-use 为空时，将 current-frame-not-in-use 按原顺序整体追加到 previous-frames 尾部并清空源列表；空帧不改变既有 LRU，迁移不改变资源有效性、管理关系或 use-context 状态。`UnusableNotification` 的非并发受保护路径现也已闭合：资源主动销毁先标记无效，管理器再恰好一次释放其 D3D 资源并 detach/remove，之后立即摘空并销毁调用时已有的上一帧 delayed-released 链；首个释放回调中新入本帧队列的资源保留到后续 flush，无重复释放、遗留注册或 use-context 状态变化。`InvalidateAndDestroyResource` 的非并发路径也已闭合为显式先失效再进入既有 `DestroyResource`：仅 active eviction 与 active-list 批量销毁使用该 helper，不处理 released free-list、不改变 use-context 深度或既有驱逐/批量顺序；释放回调观察到资源无效但仍受管理，回调请求的嵌套销毁延迟到当前资源 release、detach/remove 后执行，所有资源恰好一次释放且最终无遗留注册。`RegisterResource` 的非并发路径现也已闭合：只接受 valid 且未注册资源，non-evictable 保持 non-evictable active 归属；首次注册前已标记 evictable 的资源要求处于 use-context，先归入 current-frame-in-use，再按既有 `Use` 语义记录当前深度并保持注册顺序。管理器不执行 COM/AddRef 式寿命延长，后续销毁仍恰好一次 release、detach/remove；未改变 released free-list、`FreeSomeVideoMemory`、`DestroyAllResources` 或 `EndFrame`。`AreActiveResources` 的非并发只读查询也已闭合：仅 non-evictable、previous-frames、current-frame-not-in-use、current-frame-in-use 四类 active 归属决定结果，本帧 released 与上一帧 delayed-released 队列均不计为 active；查询不改变列表顺序、use-context 深度、资源有效性、管理关系或释放次数。资源管理器受控关闭也已按原生析构断言闭合：六类资源归属、残留注册或非零 use-context 深度均阻止关闭且无释放副作用；设备关闭先批量销毁资源再关闭管理器，use-context 未退出时在任何设备资源释放前拒绝；成功及重复关闭不重复释放，关闭后拒绝新注册。最近验证为资源管理器聚焦测试 79/79、主测试 3569/3569、ABI 8/8、Debug 四项目构建 0 警告、0 错误。
- 托管真实设备创建路径现已在 base/Ex 统一汇聚后，通过 Windows `Stdcall` slot 18 获取 `(0, 0, MONO)` 隐式 back buffer，并把该 COM 引用作为设备级 dummy 持有；失败会逆序释放 dummy、Ex、base device，正常析构按 dummy、Ex、surface 资源、base device 顺序释放。当前 render-target 非拥有身份、owning surface 释放通知及 present 失败解绑均已按原生顺序闭环；普通 `SetRenderTarget` 已闭合相同目标短路、目标变化时按需 `EndScene`、成功绑定与 viewport/matrix 后 `BeginScene` 及所有提交后失败的统一回滚。设备级 `Present` 已按原生闭合最前置 display-invalid 门控、已有 scene 的 `EndScene`、swap-chain Present、成功/occluded 后 `BeginScene` 恢复，并把 successful-present GPU marker 收归设备级所有权：仅 swap-chain 严格返回 `S_OK` 且 scene 恢复成功后记录，occluded、mode-changed、普通失败、门控失败、`EndScene` 失败和恢复失败均不记录；marker 注入接缝失败作为最终 HRESULT 返回。successful-present marker 路径现已严格保持“计数递增、时间戳获取、marker 插入”顺序；时间戳失败保留计数、返回首错且不插入 marker，WDDM 或 throttling-disabled 路径无计数、时间戳或 marker 副作用。真实 `InsertGpuMarker` 已按原生归一化 probe/create/issue 的全部 query 失败：`D3DERR_DEVICELOST`、`D3DERR_NOTAVAILABLE` 及其他失败均返回 `S_OK`，其中普通失败会在先释放当前临时 marker 后禁用并按 free、active 顺序释放现有 markers，recoverable 失败保持 marker 能力可重试；测试专用 successful-present delegate 的显式失败语义不受影响。surface render-target 不再重复 preflight 或 marker，但 discard contents 仍只在最终成功时失效。设备 capability 初始化现统一把驱动返回的 `MaxAnisotropy == 0` 规范化为 `1`，并按 mag/min anisotropic capability 缓存完整 anisotropic、min-only 或 linear fallback；基础/Ex identity 分别缓存 `D3DPOOL_MANAGED` 与内部 managed pool 6。真实设备创建汇聚现还在查询 Ex identity、获取 dummy back-buffer及提交托管所有权之前读取实际 device caps：HAL/REF 等非 SW 设备的 `PixelShaderVersion < 2.0` 返回 `WGXERR_INSUFFICIENT_GPU_CAPS` 对应 HRESULT，SW 设备保留原生例外；caps 获取失败和能力不足均逆序释放 base device，且不产生后续 COM 调用或部分提交。`S_PRESENT_OCCLUDED` 窗口唤醒边界现也已闭合：初始化保存 `NeedsRePresentOnWake` 注册消息，occluded 时在 swap-chain Present 后按 100 ms 节流、向请求中的当前呈现 HWND 单次投递并把最终 HRESULT 归一化为 `S_OK`；注册和投递返回值按原生忽略，`PresentProcessed` 保持 false，successful-present marker 与 discard contents 均不按严格成功处理，mode-changed、device-lost、普通失败及 scene 前后失败均无通知副作用。`RenderTexture` 已按调用方目标矩形和实际 texture width/height 生成 DUV2 四顶点及右/下 UV，依次设置 stage 0 typed texture、diffuse/specular material source、nearest-neighbor diffuse texture state并绘制 triangle fan；typed `SetTexture` 对非空 texture 严格先登记当前 use-context，再读取无引用 native texture 并进入 raw 状态缓存，空 texture 不登记，相同状态去重不跳过跨 use-context 使用登记。raw `SetD3DTexture` 继续保持借用指针、无额外 AddRef/Release和状态缓存职责；状态与绘制保持首错，最终仅 driver-internal-error 经 `HandleDIE` 映射，设备和 texture 均有释放后保护。`RenderTexture` blend-mode 窄承载及 lockable texture pair 双遍顺序已验证；multisample capability 探测也已确认保持 WDDM/registry 上限、depth-first 过滤和各目标格式独立回退，生产实现无需修改。验证基线：multisample 聚焦测试 7/7、全量主测试 3483/3483、ABI 测试 8/8、Debug 四项目构建 0 警告、0 错误。
